import { mount } from '@/lib/mount';
import '@/design-system/tokens.generated.css';

// Full implementation lands in Phase 5 (booking) — the 5-step booking modal
// (service → date → time → review & pay → confirmation).
mount( 'bc-booking-root', () => <></> );

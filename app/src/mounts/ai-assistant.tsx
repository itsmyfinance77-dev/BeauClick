import { mount } from '@/lib/mount';
import '@/design-system/tokens.generated.css';

// Full implementation lands in Phase 10 (AI Beauty Assistant) — global
// panel + floating launch button, wired to the beauclick-ai REST endpoints.
mount( 'bc-ai-assistant-root', () => <></> );

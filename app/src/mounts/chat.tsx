import { mount } from '@/lib/mount';
import '@/design-system/tokens.generated.css';

// Full implementation lands in Phase 9 (chat) — conversation list + thread
// view, polling beauclick-chat REST endpoints.
mount( 'bc-chat-root', () => <></> );

import { mount } from '@/lib/mount';
import '@/design-system/tokens.generated.css';

// Full implementation lands in Phase 8 (dashboards) — overview stats,
// bookings, services, availability, reviews, messages.
mount( 'bc-dashboard-professional-root', () => <></> );

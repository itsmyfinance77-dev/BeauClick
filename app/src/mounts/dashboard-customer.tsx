import { mount } from '@/lib/mount';
import '@/design-system/tokens.generated.css';

// Full implementation lands in Phase 8 (dashboards) — orders, bookings,
// wishlist, messages, loyalty, account info.
mount( 'bc-dashboard-customer-root', () => <></> );

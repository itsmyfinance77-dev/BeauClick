# کاربران — /admin/users

مسیر: `/admin/users`

## وضعیت
طراحی محدود — توکن و جزء پایه فقط.

## دلیل
جست‌وجوی شماره → اعطا/سلب نقش از قبل ساختار درستی دارد. نیاز واقعی فقط افزودن ErrorState با تلاش دوباره است (امروز ندارد) — بقیه از اجزای پایهٔ بازطراحی‌شده می‌آید.

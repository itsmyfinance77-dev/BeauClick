# V3.3 — the design's screen specs mapped onto the web app's routes

**Date:** 2026-09-21 · **Story:** #221 (conformance phase D, sizing) · **Baseline:** `master` at `d18d371`
**Inputs:** the numbered specs under `docs/design/screens/` in the design pack — `00`–`50` with no `43`, plus the `46` amendment: **50 numbered files** — and the 33 `page.tsx` files under `v3/apps/web/app/`.

This document produces the map and the sizing. It does not rebuild anything.

## How the verdicts were reached

Two kinds of evidence, and the columns say which is which.

- **Measured** — the route was rebuilt to the design and checked in a browser (`#216`, `#222`, `#211`, `#207`/`#213`). Those are recorded in `V3.3_FRONTEND_OPEN_ITEMS.md`.
- **By presence** — the route exists and has not been rebuilt. The verdict `partial` means only: *it renders in the new tokens and inside the new shell, but its own layout is the pre-design one*. It is **not** a per-screen measurement against the drawing. Each such row carries its count of inline `style={{` objects, which is the honest size of the migration that remains (`grep -c "style={{"`).

| Verdict | Meaning |
|---|---|
| ✅ conformant | Built to the spec and measured. Server gaps, if any, are named and rendered as absence. |
| ◐ partial | The route exists; layout not yet rebuilt to the design. |
| ⬜ not built | No route exists. |
| ⛔ blocked | Cannot be built as specified until a server fact, a decision or a legal text exists. |
| ⊕ cross-cutting | Not a route: a component, a rule or a set of files. |

## The map

### Customer platform

| # | Spec | Route | Exists | Verdict | Inline styles / note |
|---|---|---|---|---|---|
| 00 | HOME | `/` | yes | ✅ | `#222` |
| 01 | SEARCH | `/search` | yes | ✅ | avatar and sample count await `#226` |
| 02 | PROVIDER_PROFILE | `/providers/[id]` | yes | ✅ | completed-booking count awaits `#226` |
| 03 | CUSTOMER_DASHBOARD | `/dashboard` | yes | ✅ | join date awaits `#226`, paid amount `#225` |
| 08 | BOOKINGS_LIST | `/bookings` | yes | ◐ | 10 |
| 09 | JOURNEY | `/journey` | yes | ◐ | 25 |
| 10 | LOYALTY | `/loyalty` | yes | ◐ | 26 |
| 11 | WAITLIST | `/waitlist` | yes | ◐ | 10 |
| 15 | AUTH | `/auth` | yes | ✅ | `#222` |
| 16 | CHECKOUT_RESULT | `/checkout/result` | yes | ◐ | content and six states already match; **no visual review against the artboard**; 26 |
| 18 | NOTIFICATIONS | `/notifications` | yes | ◐ | 21 |
| 29 | PRIVACY_ACCOUNT | `/account/privacy` | **no** | ⬜ | backend exists |
| 30 | DEVICE_SESSIONS | `/account/devices` | **no** | ⬜ | backend exists |
| 35 | AI_ASSISTANT | none named | **no** | ⬜ | `/v1/me/ai/*` is `IMPLEMENTED` |
| 36 | INTERNAL_CHAT | `/messages` (proposed) | **no** | ⬜ | `/v1/chat/*` is `IMPLEMENTED` |
| 38 | WISHLIST | `/wishlist` | **no** | ⬜ | contract package exists |
| 39 | REFERRAL | `/referral` | **no** | ⬜ | contract package exists |
| 41 | CUSTOMER_COLLECTION_AND_POLICY | inside booking/checkout | **no** | ⛔ | cancellation copy renders as «در انتظار تأیید حقوقی»; refund status has no read route |
| 49 | BOOKING_NO_SHOW_AND_REMEDY | `/bookings/[id]` | **no** | ⛔ | `#212`; the spec's own R2: no read route exists for what it renders |

### Professional and business platform

| # | Spec | Route | Exists | Verdict | Inline styles / note |
|---|---|---|---|---|---|
| 04 | PRO_TODAY | `/pro` | yes | ✅ | customer name awaits `#224` |
| 05 | PRO_BOOKINGS | `/pro/bookings` | yes | ◐ | 28 |
| 06 | PRO_AVAILABILITY | `/pro/availability` | yes | ◐ | 20 → forms on `FormGrid` in `#230`; the rest remains |
| 07 | PRO_ANALYTICS | `/pro/analytics` | yes | ◐ | 10; uses the `MoneyChart` decision (24) |
| 12 | BUSINESS | `/business` | yes | ◐ | **38**; also carries the `45` staff-grant surface |
| 13 | PRO_FINANCE | `/pro/finance` | yes | ◐ | shares the finance workspace (44 inline in `finance-workspace.tsx`) |
| 14 | PRO_PROFILE | `/pro/profile` | yes | ◐ | 13 |
| 19 | PRO_SERVICES | `/pro/services` | yes | ◐ | 11 |
| 42 | SELLER_COMMERCIAL_AND_OPERATIONS | `/pro/subscription`, `/pro/credits`, `/business/operations`, `/business/staff` | **no** | ⛔ | `#210` — no ownership-scoped workspace list exists |
| 45 | OWNER_FINANCE_ACCESS | `/business` | yes | ◐ | the grant/revoke roster exists with its tests; layout is the old one |
| 46 | FINANCE_WORKSPACE | `/finance` | yes | ◐ | 44 in `finance-workspace.tsx` |
| 46a | FINANCE_WORKSPACE_AMENDMENT | `/finance` | yes | ◐ | `funds-by-state.tsx` built (`#185`); 12 inline |
| 48 | SELLER_OUTCOME_POLICY | `/pro/outcome-policy` | yes | ✅ | `#211`; 23 inline remain, and the read-only variant the spec says is "not designed" |

### Admin platform

| # | Spec | Route | Exists | Verdict | Inline styles / note |
|---|---|---|---|---|---|
| 17 | ADMIN_SETTLEMENTS | `/admin/settlements` | yes | ◐ | 19 |
| 20 | ADMIN_OVERVIEW | `/admin` | yes | ◐ | 2; the artboard is a **fully dark** page, the shell only darkened the bar |
| 21 | ADMIN_VERIFICATION | `/admin/verification` | yes | ◐ | 13 |
| 22 | ADMIN_USERS | `/admin/users` | yes | ◐ | 18 |
| 23 | ADMIN_AUDIT_LOG | `/admin/audit-log` | yes | ◐ | 15 |
| 27 | ADMIN_MEDIA_MODERATION | `/admin/media` | **no** | ⬜ | backend complete |
| 28 | ADMIN_REVIEW_MODERATION | `/admin/reviews` | **no** | ⬜ | backend complete; one open product question in the spec |
| 31 | ADMIN_PRIVACY_QUEUE | `/admin/privacy` | **no** | ⬜ | backend exists |
| 37 | ADMIN_CHAT_MODERATION | `/admin/chat-reports` | **no** | ⬜ | `bc_moderate_chat` |
| 40 | ADMIN_COMMERCIAL_CATALOGUE | `/admin/commercial/plans` … | **no** | ⬜ | 18 routes of API, no page |
| 44 | ADMIN_CONTROL_PLANE | `/admin/commercial/control` | **no** | ⬜ | collection policies + enforcement |
| 47 | ADMIN_OUTCOME_POLICY | `/admin/commercial/outcome-policies` | **no** | ⬜ | 22 routes of API, no page |
| 50 | ADMIN_COMMISSION_POLICY | `/admin/commercial/commission-policies` | yes | ✅ | `#207`, `#213`; 9 inline remain |

`/admin/loyalty`, `/admin/notifications`, `/admin/phone-conflicts`, `/admin/search` and `/sandbox-gateway` have **no spec**. Their presence is not a shortfall.

### Cross-cutting

| # | Spec | Verdict | Note |
|---|---|---|---|
| 24 | MONEYCHART_DECISION | ⊕ ⬜ | one component, two places (`/pro/analytics`, `/pro/finance`); **no `MoneyChart` exists in the app** |
| 25 | MOBILE_NAVIGATION | ⊕ ◐ | `MobileTabBar` and `AvatarMenu` built; the admin **drawer** is not (open-items §2-5) |
| 26 | ACCESSIBILITY_SPEC | ⊕ ◐ | the rules are applied where screens were rebuilt; the `§5` "requires testing" list has not been run |
| 32 | SEO_METADATA | ⊕ ⬜ | only a site-wide `metadata` in `layout.tsx`; no `robots.txt`, no sitemap, no per-route metadata |
| 33 | FOOTER_LEGAL | ⊕ ◐ | footer built; the **four legal content routes do not exist**, so the footer's links go nowhere until they do |
| 34 | TYPOGRAPHY_VAZIRMATN | ⊕ ✅ | `#216`; licensing is `#217`/`#218` |

## What the map shows

**Counts.** Of the 50 numbered specs plus the `46` amendment (51 rows): **8 ✅** routes, **21 ◐** routes, **16** with no route (**13 ⬜** and **3 ⛔**), and **6 cross-cutting**. The issue's "46 specs onto 33 routes" was the wrong shape of question — the honest answer is *not a shortfall of routes but three different gaps*:

1. **Existing routes not yet rebuilt — 21 of them.** Inline-style objects app-wide are 592 today, across routes and shared components. This is the migration `#219` describes, and it is the largest block by volume but the most predictable: same pattern each time.
2. **Routes that do not exist and whose backend does.** `/wishlist`, `/referral`, `/account/privacy`, `/account/devices`, `/messages`, the AI assistant, `/admin/media`, `/admin/reviews`, `/admin/privacy`, `/admin/chat-reports`, and the admin commercial family (`40`, `44`, `47`). These are net-new screens, each needing its own states, tests and browser check.
3. **Routes that cannot be built as drawn.** `41`, `42`, `49`. Each names its blocker in its own spec, and each blocker is already an issue (`#210`, `#212`) or a legal decision.

**Three specs describe what does not exist in code at all:** `MoneyChart`, the four legal pages, and SEO files. They are small and independent.

## Sizing

Relative points on the repository's own scale (`sp:1` … `sp:21`), **not** calibrated against velocity — the screens delivered so far were sized after the fact, and the two that turned up defects only a browser showed (see `BACKLOG_INDEX.md`, "Where verification actually caught things") each took more than their story said. Read these as *ratios between groups*, not as days.

| Group | Screens | sp | Basis |
|---|---|---|---|
| Customer pages, existing | 08, 09, 10, 11, 18, 16 | 8 | six routes, ~118 inline objects, no new server work · #231 |
| Professional pages, existing | 05, 06, 07, 14, 19 + `MoneyChart` (24) | 13 | five routes, 82 inline objects, plus one new chart component · #232 |
| Business and finance workspace | 12, 45, 46, 46a, 13 | 13 | the largest single files (`finance-workspace.tsx` 606 lines, `business` 38 inline) · #233 |
| Admin pages, existing | 17, 20, 21, 22, 23 | 8 | five routes, plus the fully-dark overview · #234 |
| Customer routes, new, backend ready | 38, 39 | 5 | two lists over existing contracts · #235 |
| Customer routes, new, backend ready | 29, 30 | 5 | account privacy and device sessions · #236 |
| Customer routes, new, backend ready | 35, 36 | 8 | chat and the AI assistant; the larger backends · #237 |
| Admin routes, new, backend ready | 27, 28, 31, 37 | 13 | four operator queues sharing one pattern · #238 |
| Admin commercial | 40, 44, 47 | 13 | 49 API routes, three screens · #239 |
| Public: SEO and legal | 32, 33 | 5 | `robots.txt`, sitemap, per-route metadata, four content pages with placeholders · #240 |
| **Total, unblocked** | | **91** | |
| Blocked | 41, 42, 49 | not pointed | `#210`, `#212`, a legal text |

## Ordering

1. **`#219`'s migration first for whatever a group touches.** A group that rebuilds a route migrates that route's inline styles in the same change; there is no separate sweep.
2. **Shells before pages** — done for the customer, professional and admin shells; the admin **drawer** remains.
3. **Existing routes before new ones.** A rebuilt route is a smaller, more certain change than a new screen and every one improves something a user already sees.
4. **New routes by user reach:** wishlist and referral (customer-facing, small), then account privacy and devices, then the operator queues.
5. **Never start a ⛔ row** — start the issue that unblocks it.

## Re-verifying this map

```bash
# routes
find v3/apps/web/app -name page.tsx | sort
# inline styles per file
grep -rc "style={{" --include=*.tsx v3/apps/web/app v3/apps/web/components | grep -v ":0" | sort -t: -k2 -nr
# specs
ls docs/design/screens 2>/dev/null   # or: git show origin/design/claude-design:…/screens
```

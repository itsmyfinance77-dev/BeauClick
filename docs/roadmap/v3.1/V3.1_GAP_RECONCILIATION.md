# V3.1 Gap Reconciliation

**Purpose:** one authoritative, re-verified statement of every currently known V3 issue,
as of the `v3.0.1` baseline. Nothing here is trusted from a prior document's claim —
every disposition below was re-checked against the source tree at `68d3d5e`.

**Baseline:** `HEAD` = `origin/master` = `68d3d5e`, tag `v3.0.1` (`5f831866`, annotated,
published). `v3.0.0` (`cfecfdf`) untouched. Nine historical V1/V2 tags untouched.
Working tree clean except one untracked local launcher, `start-beauclick.vbs`.

**Status of this document:** DISCOVERY AND RECONCILIATION ONLY. No code was written, no
migration created, no feature started, no tag cut.

**Inputs:** `V3_GAP_REGISTER.md` (779 lines, all addenda), `V3_GLOBAL_QA_REPORT.md`,
`V3_GLOBAL_UIUX_AUDIT.md`, `V3_0_1_RELEASE_RECONCILIATION.md`, `V3_RELEASE_AUDIT.md`,
`V3_RELEASE_POLICY_EXCEPTIONS.md`, `V3_ARCHITECTURE_DISCOVERY.md`, `V3_MIGRATION_PLAN.md`,
`V3_IMPLEMENTATION_ROADMAP.md`, `V3_DOMAIN_BOUNDARIES.md`, `V3_INFRASTRUCTURE_PLAN.md`,
`V3_PHASE0`–`PHASE5_IMPLEMENTATION.md`, ADR-001…025, the `v3.0.0`/`v3.0.1` tag messages,
and direct inspection of `v3/` source, migrations, tests, and CI.

---

## 0. Method, and what re-verification actually changed

Every prior register entry marked RESOLVED, OPEN, or DEFERRED was re-tested against
current code rather than carried forward on the strength of its own claim. That produced
**seven new findings and two re-classifications** that no prior document records.

| # | What re-verification found | Effect |
|---|---|---|
| R31-01 | No code path ever writes `identity.users.roles` after account creation. Every account is `['customer']` forever. | **All five admin API surfaces are unreachable.** NEW, HIGH. |
| R31-02 | `ProviderService.transitionVerification()` has no controller route — only test callers. | The `verified` badge, `verifiedOnly` filter, and `WEIGHT_VERIFIED` ranking term are permanently inert. NEW, MEDIUM-HIGH. Same class as QA-18, never recorded. |
| R31-03 | Zero file-upload / object-storage capability exists anywhere in `v3/`. | Portfolio, avatars, and verification evidence are blocked on infrastructure nobody has built. NEW, HIGH as a prerequisite. |
| R31-04 | `EXC-001` is scoped to "the `v3.0.0` release only" and its own review condition fires on any successor release while GAP-06b is open. `v3.0.1` shipped recording EXC-001 as "unchanged and unextended". | `v3.0.1` has no covering release exception. Process gap; must be settled before `v3.1.0`. NEW. |
| R31-05 | `identity` schema has 4 of the 8 tables `V3_DOMAIN_BOUNDARIES.md` specifies (`roles`, `capabilities`, `sessions`, `business_account_approvals` absent). | Where R31-01's fix lands. NEW, informational. |
| R31-06 | No `referral`, `ai`, `admin`, or `privacy` schema or module exists, though all four are fully specified in `V3_DOMAIN_BOUNDARIES.md`. | Restated as capability absence, not just "not built". |
| R31-07 | No campaign domain exists in V3 at all — `commerce` has `order_adjustments` and exactly one registered rule (`MembershipDiscountRule`). | **GAP-04 and GAP-19 are re-classified**: they describe a V2 domain V3 never ported. See §5. |

Two re-classifications follow from R31-07 and are stated explicitly rather than left to
inference: **GAP-04** (campaign usage-cap TOCTOU) and **GAP-19** (B2B campaign
eligibility) have been carried forward as "OPEN, unchanged, out of scope" through every
phase addendum and through `V3_0_1_RELEASE_RECONCILIATION.md` §5. That reads as though a
race condition is live in V3. It is not — there is no campaign code in V3 to race. They
are **SUPERSEDED**, replaced by a single product question (does V3 want campaigns at all)
tracked as `R31-07`.

---

## 1. Classification key

**Type** — `BUG`, `SECURITY`, `DATA INTEGRITY`, `UX`, `ACCESSIBILITY`, `PERFORMANCE`,
`PRODUCT GAP`, `BUSINESS DECISION`, `EXTERNAL CONFIGURATION`, `EVIDENCE-GATED`,
`DEFERRED`, `INFORMATIONAL`.

**Status** — `OPEN`, `PARTIALLY_RESOLVED`, `RESOLVED`, `DEFERRED`, `EXTERNAL`, plus
`SUPERSEDED` where re-verification found the item describes something V3 does not have.

**Complexity** — `XS` (< 1 day), `S` (1–3 days), `M` (1–2 weeks), `L` (2–4 weeks),
`XL` (> 4 weeks or externally gated).

**Phase** — the recommended V3.1 phase from `V3.1_PRODUCT_ROADMAP.md`, or `V3.2+` /
`NONE`.

---

## 2. Master index — open and actionable items

Sorted by recommended phase, then severity. Detail for each is in §4.

| ID | Title | Domain | Status | Type | Sev | Cx | Phase |
|---|---|---|---|---|---|---|---|
| **R31-01** | Privileged roles are ungrantable; the entire admin API is unreachable | Identity / Admin | OPEN | PRODUCT GAP / OPERATIONAL | HIGH | M | A |
| **R31-02** | Verification workflow has no caller; the `verified` signal is inert | Provider / Search | OPEN | PRODUCT GAP | MED-HIGH | M | A |
| **GAP-20** | No admin UI for resolving a recorded phone conflict | Auth / Admin | OPEN | PRODUCT GAP | LOW | S | A |
| **GAP-24** | No admin UI for tuning ranking weights | Search / Admin | DEFERRED | PRODUCT GAP | LOW | S | A |
| **GAP-02-V3** | Audit logging is structured-logger-based, not DB-persisted or structurally enforced | Cross-cutting | OPEN | SECURITY / DATA INTEGRITY | MED | M | A |
| **R31-05** | `identity` schema missing `roles`/`capabilities`/`sessions`/`business_account_approvals` | Identity | OPEN | PRODUCT GAP | INFO | — | A |
| **ADMIN-UI** | No admin frontend exists (5 gated API surfaces, zero pages) | Admin | OPEN | PRODUCT GAP | HIGH | L | A |
| **PRO-UI** | No professional-specific frontend; ~40% of built API has no UI | Frontend | OPEN | PRODUCT GAP | HIGH | L | B |
| **QA-25** | Business nav link shows to every authenticated user | Frontend | OPEN | UX | LOW | XS* | B |
| **GAP-13** | Independent solo professional has no staff/delegation concept | Provider | PARTIALLY_RESOLVED | PRODUCT GAP | LOW | M | B / V3.2+ |
| **R31-03** | No file upload or object storage capability exists anywhere | Infrastructure | OPEN | PRODUCT GAP | HIGH | M | C |
| **GAP-23** | Portfolio absent; V2's half-built version must be redesigned, not ported | Provider | DEFERRED | PRODUCT GAP | HIGH | L | C |
| **IMAGERY** | Zero images in the entire product | Frontend / Provider | OPEN | PRODUCT GAP | HIGH | L | C |
| **REVIEWS** | No review domain exists | Provider | OPEN | PRODUCT GAP | HIGH | L | D |
| **QA-18** | Rating signals have no writer; badge and ranking term permanently inert | Search | OPEN | PRODUCT GAP | MED | S* | D |
| **QA-22** | Label maps fall back to raw English keys | Frontend | OPEN | INFORMATIONAL | INFO | XS | D |
| **QA-17** | Vazirmatn named but never shipped | Frontend | OPEN | UX / BUSINESS DECISION | MED | S | E |
| **QA-23** | No footer anywhere; no terms/privacy/contact/support route | Frontend / Legal | OPEN | UX / BUSINESS DECISION | MED | S* | E |
| **GAP-21** | No deletion cooldown / grace period | Privacy | OPEN | BUSINESS DECISION | MED | S | E |
| **GAP-22** | No self-service data export or deletion for anyone | Privacy | OPEN | PRODUCT GAP / LEGAL | HIGH | L | E |
| **QA-16r** | Homepage is an honest placeholder, not a designed landing page | Frontend | OPEN | UX | MED | M | E |
| **GAP-09** | SEO: no favicon, OG/Twitter metadata, robots, or sitemap in V3 | SEO | PARTIALLY_RESOLVED | PRODUCT GAP | MED | S | E |
| **QA-19** | No resend-OTP affordance and no expiry countdown | Auth / Frontend | OPEN | UX | LOW | S | E |
| **QA-20** | `GET /v1/auth/sessions` returns `current: false` hardcoded | Auth | OPEN | BUG | LOW | S | E |
| **GAP-06b** | No real production payment gateway | Payment | OPEN / EXTERNAL | EXTERNAL CONFIGURATION | **BLOCKING (prod)** | XL | F |
| **QA-21** | Cancel and decline conflated on the payment result page | Payment / Frontend | OPEN | UX | LOW | S | F |
| **HOSTING_GRANTS** | Ledger role contract proven only against CI's ephemeral container | Infrastructure | EXTERNAL | EVIDENCE-GATED | HIGH | S | F |
| **PHASE4-03** | PostgreSQL 15+ revoked default `public` CREATE grant | Infrastructure | OPEN | EXTERNAL CONFIGURATION | MED | XS | F |
| **R31-04** | `EXC-001` does not cover `v3.0.1` and does not extend to `v3.1.x` | Release process | OPEN | BUSINESS DECISION | MED | XS | F |
| **HOSTING** | Hosting/region decision never made — the standing Phase 0 exit blocker | Infrastructure | OPEN | BUSINESS DECISION | HIGH | — | F |
| **UIKIT** | 6-primitive UI kit; almost all layout is inline `style={{}}` | Frontend | OPEN | UX / TECHNICAL DEBT | MED | M | G |
| **A11Y-CONTRAST** | Colour contrast never measured | Frontend | OPEN | ACCESSIBILITY | MED | S | G |
| **TOUCH-CLASS** | 44px baseline enforced in `Button` only; five recurrences of the same bug | Frontend | OPEN | ACCESSIBILITY | MED | S | G |
| **GAP-10** | Every business-tunable numeric policy is provisional | Cross-cutting | OPEN | BUSINESS DECISION | MED | — | All |
| **THROTTLE-STORE** | Throttler storage is in-memory per process | Infrastructure | OPEN | PERFORMANCE / SECURITY | MED | S | F |
| **GAP-11** | AI/SMS/email providers never exercised against a real API | Notifications | OPEN / EXTERNAL | EXTERNAL CONFIGURATION | MED | M | E / V3.2+ |
| **GAP-12** | AI conversation cardinality — one thread per tenant | AI | DEFERRED | PRODUCT GAP | — | — | V3.2+ |
| **AI** | No AI domain exists | AI | OPEN | PRODUCT GAP | MED | XL | V3.2+ |
| **REFERRAL** / **GAP-28** | No referral domain exists | Referral | OPEN | PRODUCT GAP | MED | L | V3.2+ |
| **WISHLIST** | No wishlist domain exists | Provider | OPEN | PRODUCT GAP | LOW | M | V3.2+ |
| **CRM** / **GAP-16** | No CRM domain exists; V2's is explicitly not portable as-is | Booking | OPEN | PRODUCT GAP | MED | L | V3.2+ |
| **B2B** / **GAP-19** | No B2B quote domain exists | Commerce | SUPERSEDED | PRODUCT GAP | LOW | L | V3.2+ |
| **R31-07** | No campaign/promotion domain exists in V3 | Commerce | OPEN | BUSINESS DECISION | MED | L | V3.2+ |
| **GAP-18** | No automated payout/disbursement | Financial | DEFERRED | PRODUCT GAP | MED | L | V3.2+ |
| **GAP-25** | No calendar sync or live-recurrence availability engine | Booking | DEFERRED | PRODUCT GAP | LOW | L | V3.2+ |
| **GAP-26** | Waitlist is deliberately never a reservation/auction system | Booking | DEFERRED | INFORMATIONAL | — | — | NONE |
| **GAP-27** | Ranking is hand-weighted, not ML | Search | DEFERRED | INFORMATIONAL | — | — | NONE |
| **QA-26** | `next dev` invalidates its own stylesheet URL | Tooling | OPEN | INFORMATIONAL | INFO | — | NONE |

`Cx` marked `*` means the *frontend* change is trivial but it is blocked on a backend
signal that does not exist — see the item's detail entry.

---

## 3. Closed items — roll-up, re-verified not re-litigated

Every item below was confirmed closed against current source in this pass. They are
listed so this document is a complete census rather than a to-do list, and so a reader
does not have to consult five addenda to establish that a gap is genuinely gone.

| IDs | Domain | How closed | Re-verified how |
|---|---|---|---|
| GAP-01 | Financial | `financial` schema owned by a NOLOGIN role; app connects as `beauclick_financial_writer` with `INSERT`+`SELECT` only; main role has `REVOKE ALL ON SCHEMA financial` | `database/migrations/financial/…`, `database/scripts/financial-roles.sql`, `financial-integrity.pg-spec.ts` all present |
| GAP-02 (V2 instance) | Admin | V2's `set_tiers` fixed and structurally guarded in the WordPress codebase | Applies to V2 only; the **V3 successor gap is open** — see `GAP-02-V3` in §2 |
| GAP-03 | Commerce | `uq_orders_source` on `(source_type, source_id)`; booking+order in one transaction | Migration confirmed present |
| GAP-05 | Financial | `MyFinanceService` takes a session user id and nothing else | `financial.controller.ts` confirmed to accept no party argument on any `v1/me/finance` route |
| GAP-06a | Payment | `SandboxPaymentProvider`, full lifecycle, CI-proven | `payment/20260822300001_sandbox_payment_provider.sql`, provider source present |
| GAP-08 | Authorization | `require_owner_or_capability` gained an owner-resolver parameter (V2); V3's equivalent is `@ResolveOwner` + `libs/ownership` | Four resolver classes confirmed in `services/business`, `services/provider` |
| GAP-14 | Search | OpenSearch with a verified Persian analyzer chain | `services/search/src/opensearch/`, `provider-index.definition.ts` present |
| GAP-15 | Search | `ProviderProfileViewed.entityType` is `z.literal('provider')` + a CHECK constraint | `libs/event-contracts/src/catalog/search.events.ts` confirmed |
| GAP-17 | Notifications | Real notification centre with list/unread/mark-read/preferences | `services/notification`, `/notifications` route confirmed |
| GAP-29 | Journey | Standalone module, ADR-019 | `services/journey` + `journey` schema confirmed |
| PHASE2-01…06, PHASE4-01…04, PHASE5-01…03 | Various | Each closed in its own phase; PHASE4-03 is the one exception and remains open (§2) | Spot-checked; no regressions |
| QA-01…QA-16, QA-24 | Various | 17 fixes landed, CI run `32721273257` green on `5833610`, tagged `v3.0.1` | `git log` confirms all seven commits are ancestors of `v3.0.1` |
| PHASE5-02 | Security | One registered throttler + per-route `@Throttle(policy(...))`, `/health` exempt | `.env.example` rate-limit block and `throttling.pg-spec.ts` confirmed |

---

## 4. Detail — every open item

Format per item: current implementation state (verified), evidence, the four impact
axes, dependencies, complexity, recommended phase.

### R31-01 · Privileged roles are ungrantable; the entire admin API is unreachable · **NEW · HIGH**

- **Domain:** Identity / Admin · **Status:** OPEN · **Type:** PRODUCT GAP / OPERATIONAL
- **Current implementation state.** `AccountResolverService.resolveOrCreate()`
  (`services/identity/src/account/account-resolver.service.ts:34`) creates every user with
  `roles: ['customer']`. A repo-wide grep for any write to `roles` after creation returns
  **nothing**: `MeController.updateMe` touches `displayName` only, and no other service,
  controller, migration, or seed writes the column. `database/seeds/` contains one file,
  `reference-data.seed.ts` (cities and specialties).
- **Evidence.** `CAPABILITIES_BY_ROLE` (`services/identity/src/rbac/capabilities.ts`)
  grants `bc_manage_platform` only to `platform_operator` and `administrator`,
  `bc_moderate_verification`/`bc_moderate_reviews` only to `moderator` and
  `administrator`. Five admin controller groups exist and every route is
  `@RequireCapability('bc_manage_platform')`: `/v1/admin/analytics`, `/v1/admin/finance`,
  `/v1/admin/loyalty`, `/v1/admin/notifications`, `/v1/admin/search`.
- **What is actually unreachable.** Platform analytics; cross-party financial totals;
  settlement creation and settlement **reversal**; loyalty policy inspection (the endpoint
  `GAP-10` relies on to make provisional values visible); notification delivery status and
  retry-due; search **reindex** and **rebuild-projection** — i.e. the only recovery path
  for a corrupted or stale search index.
- **User impact.** None directly; this is invisible to customers until something needs
  operating.
- **Security impact.** Neutral-to-positive today (nothing over-privileged exists), but the
  eventual fix creates the platform's first privileged principal and must be designed as
  such — audit-logged, least-privilege (`platform_operator` by default, per
  `V3_SECURITY_MODEL.md` §9), and never a static seeded superuser.
- **Revenue impact.** Settlement cannot be run, so professionals cannot be paid out
  through any product path.
- **Operational impact.** **Total.** The platform cannot be administered at all except by
  hand-editing `identity.users.roles` in the database. That is the definition of an
  unoperable system.
- **Dependencies.** None upstream. Blocks: Admin UI, review moderation, R31-02, QA-25,
  GAP-20, GAP-24.
- **Complexity.** M. Either build `identity.roles`/`identity.capabilities` per
  `V3_DATABASE_BLUEPRINT.md` §8, or ship a narrower grant path plus a bootstrap
  mechanism. The register has flagged the code-based simplification since Phase 1; this
  finding is that the simplification has a consequence nobody costed.
- **Phase:** A.

### R31-02 · Verification workflow has no caller; the `verified` signal is inert · **NEW · MEDIUM-HIGH**

- **Domain:** Provider / Search · **Status:** OPEN · **Type:** PRODUCT GAP
- **Current implementation state.** `ProviderService.transitionVerification()`
  (`services/provider/src/provider.service.ts:141`) is a complete, CAS-hardened,
  event-emitting state machine. Its only callers in the entire workspace are four lines in
  `apps/api/test/search-projection.pg-spec.ts`. `ProviderController` exposes six routes;
  none of them is a verification route. There is no `verification_requests`,
  `verification_evidence`, or `verification_history` table — `V3_DOMAIN_BOUNDARIES.md`
  §provider names all three.
- **Consequence.** Every professional is `unverified` permanently.
  `SearchProvidersDto.verifiedOnly` therefore matches nothing;
  `RankingConfig.WEIGHT_VERIFIED * verified` is always `0`; the `verified` badge is never
  awarded. This is structurally identical to **QA-18** (rating signals with no writer) and
  was never recorded — the QA pass found the rating half and not this one.
- **User impact.** Customers get no trust signal at all in a marketplace where trust is
  the purchase decision. Two of the five ranking badge keys are unreachable, so ranking is
  flatter than designed.
- **Security impact.** None directly. Indirect: an unverified-by-default marketplace with
  no verification path means nobody has checked that a listed professional is real.
- **Revenue impact.** Medium — verification is a standard conversion lever and a standard
  premium/priority-placement lever.
- **Operational impact.** High — there is no moderation queue, so the platform cannot
  perform its own core supply-side quality control.
- **Dependencies.** R31-01 (needs `bc_moderate_verification` to be grantable);
  R31-03 (evidence upload needs object storage).
- **Complexity.** M — routes, request/evidence tables, a moderation queue, and the admin
  screen. The state machine and its event already exist and are tested.
- **Phase:** A.

### R31-03 · No file upload or object storage capability exists anywhere · **NEW · HIGH (prerequisite)**

- **Domain:** Infrastructure · **Status:** OPEN · **Type:** PRODUCT GAP
- **Current implementation state.** A repo-wide search of `v3/` for `S3`, `multer`,
  `presign`, and `upload` returns **zero** matches outside `node_modules`. There is no
  `public/` directory in `apps/web`. `V3_IMPLEMENTATION_ROADMAP.md` Phase 1 listed
  *"Object storage wired for portfolio media + verification evidence"* as a deliverable;
  it was never built and no phase report records dropping it.
  `V3_INFRASTRUCTURE_PLAN.md` §5 names an S3-compatible, Iran-reachable provider as the
  strategy, with no provider chosen.
- **User impact.** Indirect but total: it is why the product has zero images (§IMAGERY).
- **Security impact.** Designing this is where the protected-file model in
  `V3_SECURITY_MODEL.md` §8 has to become real — verification evidence is sensitive
  personal documentation and must never be publicly addressable, including after a
  backup restore (`V3_INFRASTRUCTURE_PLAN.md` §9).
- **Revenue impact.** High, via IMAGERY — a beauty marketplace where customers cannot see
  work is not competitive.
- **Operational impact.** Medium — media lifecycle, quota, and abuse handling are new
  operational surfaces.
- **Dependencies.** Hosting/region decision (`HOSTING`) — provider choice is downstream of
  it. Blocks: GAP-23 (Portfolio), IMAGERY, R31-02's evidence upload.
- **Complexity.** M for the adapter + presigned-upload path + validation; the abuse,
  quota, and moderation policy around it is where the real cost is.
- **Phase:** C, and it is the gate for the whole phase.

### R31-04 · `EXC-001` does not cover `v3.0.1` and does not extend to `v3.1.x` · **NEW · MEDIUM**

- **Domain:** Release process · **Status:** OPEN · **Type:** BUSINESS DECISION
- **Current implementation state.** `V3_RELEASE_POLICY_EXCEPTIONS.md` states EXC-001
  "Applies to: The `v3.0.0` release only", and its Review condition fires when *"any
  subsequent release (`v3.0.x`, `v3.1.0`, …) is proposed while GAP-06b is still open — the
  exception covers `v3.0.0` **only** and does not automatically extend to successors."*
  The `v3.0.1` tag message records `EXC-001 = STILL ACTIVE, unchanged and unextended`.
- **Reading it honestly.** That sentence is accurate about EXC-001 and, read together with
  the exception's own scope clause, means `v3.0.1` was released **without a covering
  release exception**. Nothing harmful followed — `v3.0.1` changes no payment behaviour and
  the production gate still fails closed — but the exception index no longer describes the
  release history, and the same condition will fire again, more consequentially, at
  `v3.1.0`.
- **Impact.** No user, security, or revenue impact. Operational/governance impact only:
  the document that exists specifically to prevent silent policy drift has drifted.
- **Dependencies.** None. Resolution is a decision, not engineering: either record
  `EXC-002` covering the `v3.0.x`/`v3.1.x` line, or re-scope EXC-001 with an explicit,
  dated amendment. **Do not let it lapse by convention** — that is the exact failure mode
  the document forbids.
- **Complexity.** XS.
- **Phase:** F, but it must be settled *before* any V3.1 tag is cut, not with the gateway.

### R31-05 · `identity` schema missing four specified tables · **NEW · INFORMATIONAL**

`identity` has `users`, `otp_requests`, `refresh_tokens`, `phone_conflicts`.
`V3_DOMAIN_BOUNDARIES.md` §identity also names `roles`, `capabilities`,
`sessions`/`refresh_tokens`, and `business_account_approvals`. The first two are R31-01's
landing site. `sessions` is partially covered by `refresh_tokens` (and its absence is why
QA-20 cannot be fixed without a JWT claim). `business_account_approvals` is unnecessary
under ADR-023, which resolves business ownership by row rather than by an approval
workflow — recorded so the divergence is deliberate rather than forgotten. **Phase A.**

### R31-06 · Four fully-specified domains have no schema or module · **NEW · INFORMATIONAL**

`referral`, `ai`, `admin` (`admin_audit_log`), and `privacy` (`data_requests`) each have a
complete responsibility, schema, API, event, and data-ownership specification in
`V3_DOMAIN_BOUNDARIES.md` and **zero** implementation. This is not a defect — none was in
any delivered phase's scope — but it means four of the fourteen designed domains are
absent, and the design work for each is already done and does not need repeating. See
`V3.1_PRODUCT_ROADMAP.md` §21 for the reuse assessment.

### R31-07 · No campaign/promotion domain exists in V3 · **NEW · MEDIUM · supersedes GAP-04 and GAP-19**

- **Current implementation state.** `commerce` has `orders`, `order_items`,
  `order_adjustments`, `outbox_events`. There is no `campaigns`, `campaign_usages`, or
  `b2b_price_tiers` table. `PricingService` takes an injected `PRICING_RULES` array and
  exactly one rule is registered anywhere: `apps/api/src/pricing/membership-discount.rule.ts`.
  Every other "Campaign" mention in `v3/` is a comment describing the design.
- **Why this is a re-classification.** GAP-04 describes a confirmed TOCTOU race in V2's
  `CampaignService`. GAP-19 describes B2B orders being excluded from campaign eligibility.
  Both have been carried through every V3 phase addendum as OPEN. Neither can be true of
  V3, which has no campaign code. Continuing to list them as open V3 gaps overstates V3's
  risk surface and understates its **scope** gap.
- **The real question,** which is a business one: does V3 want promotional pricing at all,
  and if so is it a launch capability or a growth-phase one? V2 had it, V3 dropped it
  silently, and no document records the decision.
- **Impact.** Revenue/marketing capability absent; no security or integrity risk (there is
  nothing to exploit). The pricing chain was explicitly designed to accept it additively.
- **Complexity.** L. **Phase:** V3.2+, pending the business answer.

### GAP-06b · No real production payment gateway · **OPEN / EXTERNAL · PRODUCTION-BLOCKING**

Unchanged in substance and re-verified: `PAYMENT_DEFAULT_PROVIDER=sandbox`,
`PAYMENT_ENVIRONMENT=sandbox`, `SandboxPaymentProvider` the only registered provider, no
merchant credential of any kind in `.env` or `.env.example`, no adapter file. The
production gate is two independent conditions with no override, re-read and confirmed.
**Revenue impact: absolute — no real money can move.** User impact: none today (nothing is
deployed). Security impact: currently positive (fail-closed). Operational impact: blocks
launch entirely. Dependencies: real merchant credentials (external), the hosting decision
(callback reachability), and the `failureCode` redirect-contract change QA-21 needs — all
three should be settled in one pass, not three. Complexity: XL, externally gated.
**Phase F.** `QA-10` has already reduced its risk surface by making the amount check
currency-aware before an adapter exists; that was the right order.

### HOSTING_GRANTS · Ledger role contract proven only against CI · **EXTERNAL · EVIDENCE-GATED · HIGH**

`database/scripts/financial-roles.sql` has been verified only against CI's ephemeral
PostgreSQL 16 container. `PHASE4-03` (PostgreSQL 15+ revokes the default `CREATE ON SCHEMA
public` grant) is a live consideration on any managed provider. Both are one action:
provision the real target host and run the script. **This is a deployment prerequisite of
equal standing to GAP-06b** and should not be discovered on launch day. Complexity S once
a host exists; blocked on `HOSTING`. **Phase F.**

### HOSTING · The hosting/region decision has never been made · **OPEN · BUSINESS DECISION · HIGH**

`V3_INFRASTRUCTURE_PLAN.md` §1 calls this *"the one deliberately open item in this entire
blueprint"* and *"a Phase 0 exit blocker"*. Phase 0 exited, five phases shipped, and two
releases were cut without it. It is coupled to three other decisions and must be resolved
as one: AI-provider reachability, payment-gateway callback reachability, and object-storage
provider (R31-03). **This is the single highest-leverage unresolved decision in the
project** — four separate V3.1 items are downstream of it. Complexity: a decision, not
work. **Phase F, but it should be started on day one of V3.1, not at F.**

### PRO-UI · No professional-specific frontend · **OPEN · HIGH**

- **Current implementation state, measured.** The web app consumes 24 distinct API paths.
  The API exposes far more. Endpoints that are **built, tested, and gated but have no UI
  at all**: `GET/POST/DELETE /v1/me/availability/slots`, `/slots/bulk`,
  `GET /v1/me/professional-bookings`, `POST /v1/bookings/:id/complete`, `/no-show`,
  `/reschedule`, `GET /v1/bookings/:id/history`, `POST /v1/providers` (create profile),
  `PATCH /v1/providers/:id`, `POST /v1/providers/:id/services`, all of
  `/v1/me/finance/*` (summary, outstanding orders, settlements, per-order ledger), all of
  `/v1/me/analytics/*`, `/v1/me/loyalty/tiers`, `/v1/me/loyalty/membership`,
  `GET /v1/auth/sessions`, `POST /v1/auth/logout-all-devices`, and every `/v1/admin/*`
  route.
- **What that means concretely.** A professional cannot, through the product: create or
  edit their profile, publish availability, list their own bookings, mark a booking
  complete or no-show, reschedule, see what they have earned, or see any analytics.
  `/dashboard` is a three-field shared identity page.
- **User impact.** The supply side of a two-sided marketplace cannot operate. This is the
  largest single gap in the product.
- **Revenue impact.** Direct — no completed bookings means no commission, and completion
  is a professional action with no UI.
- **Operational impact.** Every professional interaction would have to be performed by
  support on their behalf, which is not possible either (R31-01).
- **Dependencies.** None on the backend — this is the rare item where the expensive half
  is already built, tested against real PostgreSQL, and secured with ownership resolvers.
  Benefits from G (UI kit) running alongside.
- **Complexity.** L, but low-risk: no new API, no migration, no new security surface.
- **Phase:** B. **This is the highest value-per-unit-of-risk work in the entire plan.**

### ADMIN-UI · No admin frontend · **OPEN · HIGH**

Zero admin pages. The API half exists and is correctly gated on every route (verified by
enumerating all seventeen controllers). Minimum operational set, in the order
`V3_IMPLEMENTATION_ROADMAP.md` Phase 4 itself recommends under time pressure:
**verification queue** (R31-02), **role/capability management** (R31-01), **audit log
viewer** (GAP-02-V3), **financial settlement + reversal**, **search reindex / index
health**, **notification delivery status**, **phone-conflict resolution** (GAP-20),
**loyalty policy** (GAP-10 visibility). Deliberately *not* minimum: ranking-weight tuning
(GAP-24), review moderation (needs D), user impersonation (never). Complexity L.
**Phase A.**

### REVIEWS · No review domain exists · **OPEN · HIGH**

No entity, no table, no write path, no `ReviewCreated` contract in
`libs/event-contracts/src/catalog/`. `booking.service.ts:383` still describes reviews as
something *"later phases … will consume"*. `bc_moderate_reviews` exists as a capability
with nothing behind it. `V3_DOMAIN_BOUNDARIES.md` places reviews in the `provider` schema
with a `ReviewCreated` v1 event consumed by search and loyalty — that design is done.
User impact: customers have no quality signal and no voice; professionals have no
reputation asset. Revenue: reviews are the primary conversion driver in this category.
Dependencies: `BookingCompleted` (exists), moderation capability (A), professional
response UI (B). Complexity L. **Phase D.** Closes QA-18 as a consequence, and makes
`LOYALTY_POINTS_REVIEW_SUBMITTED` (already configured at 5) meaningful.

### QA-18 · Rating signals have no writer · **OPEN · MEDIUM**

`search.ranking_signals.rating_sum` / `.review_count` are written by nothing; the
migration says so in a comment. `ratingAvg` is permanently 0, `high_rating` unreachable,
the Bayesian term always collapses to cold-start. Blast radius limited today because the
frontend exposes neither `minRating` nor the `rating` sort. **The fix is a consumer, not a
feature** — an S-sized signal writer on `ReviewCreated`, using the existing
`search.signal_applications` idempotency table. **Phase D**, bundled with REVIEWS.

### GAP-22 / GAP-21 · Privacy: no export, no deletion, no anonymization, no cooldown · **OPEN · HIGH / LEGAL**

Verified absent: no export endpoint, no deletion endpoint, no anonymization path.
`identity.users.deletedAt` exists as a column and **nothing ever sets it**.
`confirm_deletion` exists only as an OTP purpose. `V3_DOMAIN_BOUNDARIES.md` specifies the
correct design — a thin `privacy` orchestrator calling each module's own self-registered
`exportSubjectData(userId)` / `eraseSubjectData(userId)`, which closes V2's `PRIV-06`
hardcoded-call-list blind spot by construction. Fourteen domains own user data; a
hand-maintained list would go stale immediately, so the self-registration property is the
whole design and must not be traded away for speed. GAP-21 (deletion grace period) is a
**business decision** riding on the same work. User impact: users cannot get their data
or leave. Security: erasure is also a breach-blast-radius control. **Legal: this is the
item with actual regulatory weight, and it is not currently on anyone's critical path.**
Complexity L. **Phase E.**

### GAP-02-V3 · Audit logging is logger-based, not DB-persisted or structurally enforced · **OPEN · MEDIUM**

V2's `GAP-02` was closed by a boot-time route-registration guard that made an unaudited
admin mutation impossible to register. V3 has no equivalent: `AuditLogService` writes to
the structured logger, there is no `admin.admin_audit_log` table, and nothing prevents a
new admin route from shipping unaudited. `V3_DOMAIN_BOUNDARIES.md` specifies the
structural contract (`libs/audit`, every mutation-capable controller must call
`AuditLog.record` or fail to register). The gap register calls this the "recurring bug
class, not a one-off" — three separate instances in V2 across two plugins. Building the
admin UI (A) without this rebuilds the conditions for the fourth. Security/data-integrity
impact: an unauditable privileged action is unattributable. Complexity M. **Phase A —
same phase as the first privileged principal, deliberately.**

### QA-17 · Vazirmatn named but never shipped · **OPEN · MEDIUM · BUSINESS DECISION**

`--bc-font-family: 'Vazirmatn', …` with no `@font-face`, no `next/font`, no font file, no
`public/`. Every user without it locally installed gets a Latin-first system fallback on
every screen of a Persian-only product. The UI/UX audit's #1 recommendation and its own
words: *"the one finding here that affects literally every pixel of Persian text."* The
blocker is a decision — self-host (~100KB+ per weight, into repo and build) vs CDN (an
external origin with genuine availability problems for an Iranian audience). Prior
recommendation, which this pass endorses: **self-host a Latin+Arabic subset at 400/600/700
via `next/font/local`.** Complexity S once decided. **Phase E**; it is the cheapest
high-impact item in the plan and could reasonably be pulled earlier.

### QA-23 · No footer, no terms, no privacy, no contact, no support · **OPEN · MEDIUM · BUSINESS DECISION**

`AppShell` is `<header>` + `<main>`; landmarks confirmed live as exactly `HEADER, NAV,
MAIN`. The markup is XS; the **content is the blocker** — terms of service, privacy
policy, contact, and support are legal artifacts someone must author and approve. For a
platform that takes payments and holds personal data this is a commercial and probably
regulatory gap. Shipping an empty shell or links to 404s is worse than shipping nothing.
Pairs naturally with GAP-22 (the privacy policy and the privacy *mechanism* should be
written together). **Phase E.**

### GAP-09 · SEO, frontend half · **PARTIALLY_RESOLVED · MEDIUM**

The V2 discovery-coverage half is closed. The V3 frontend half is not: `layout.tsx` sets
only `title` and `description` — no favicon, no apple-touch-icon, no OpenGraph or Twitter
metadata, no `robots`, no `sitemap`. Any link shared into a messaging app renders bare, in
a market where sharing happens in messaging apps. Also still open and never settled: the
Persian-slug `post_name` question, which needs a live URL check rather than more code
reading, and the pretty city/specialty path decision. Complexity S for metadata,
M including SSR/slug decisions. **Phase E.**

### QA-16r · Homepage is a placeholder, not a landing page · **OPEN · MEDIUM**

QA-16 removed the Phase 1 scaffold copy; it did not design a homepage. There is no hero,
no value proposition, no category entry points, no social proof, no imagery. Depends on C
(imagery) to be worth doing properly. Complexity M. **Phase E.**

### QA-19 / QA-20 / QA-21 / QA-25 · The four "looks trivial, isn't" items

Each was correctly excluded from `v3.0.1` because the cheap version is worse than the
current state. Restated with what each actually needs:

| ID | Needs | Phase |
|---|---|---|
| QA-19 resend OTP | The API to return remaining cooldown seconds so the button can count down instead of failing with an unanticipated `RATE_LIMITED` | E |
| QA-20 `current: false` | A session identifier claim in the JWT — a token-shape change affecting every issued token | E (with device management) |
| QA-21 cancel vs decline | `failureCode` threaded through the gateway redirect contract — **settle once, with the real adapter, not twice** | F |
| QA-25 business nav link | A business-membership signal on `/v1/me`, or dynamic roles (R31-01) | B (falls out of A) |

### UIKIT / TOUCH-CLASS / A11Y-CONTRAST · The component-layer items · **OPEN · MEDIUM**

Six UI primitives; almost all layout is inline `style={{}}` objects. The UI/UX audit's
diagnosis is the important part and this pass agrees with it: **five independent
empty-state bugs and five independent 44px touch-target bugs are symptoms of a thin
component layer, not of carelessness.** 44px is enforced inside `Button` and nothing
enforces it for a `Link`, so each new link surface re-learns it. Colour contrast has never
been measured — the palette is `oklch` with plausible separation, but no ratio has been
computed, and it is the most significant unverified accessibility dimension. No modal or
dialog exists anywhere, so the first one added will need focus management with no
precedent in the codebase. Complexity M. **Phase G, running alongside B** — building the
professional surface on the current component layer would manufacture the sixth and
seventh instance of both bug classes.

### GAP-10 · Every business-tunable numeric policy is provisional · **OPEN · BUSINESS DECISION**

Unchanged and correctly handled: made *visible* rather than silently settled. Every value
is environment-configurable and `GET /v1/admin/loyalty/policy` reports which are still V2
placeholders — though R31-01 means nobody can currently call that endpoint. Full list in
`V3.1_PRODUCT_ROADMAP.md` §16. **Phase: all**, as a decision track running parallel to
engineering.

### THROTTLE-STORE · In-memory per-process throttler storage · **OPEN · MEDIUM**

At multi-instance scale the effective limit multiplies by instance count. Correct fix is a
shared Redis store; deliberately not adopted at single-instance scale. Becomes real the
moment the hosting decision produces more than one instance, so it belongs **with** that
decision. **Phase F.**

### GAP-11 · SMS / email / AI providers never exercised live · **OPEN / EXTERNAL · MEDIUM**

Handled well: channels report `providerVerified: false` so a logging provider can never be
mistaken for a delivering one. But it means **no notification the product sends actually
reaches anyone off-platform** — including the OTP that is the only way to log in. In-app
delivery is genuinely real; SMS and email are not. Blocked on credentials (external) and
coupled to the hosting/region decision. **A real SMS provider is a launch prerequisite,
not a nice-to-have** — without it nobody can authenticate. Complexity M. **Phase E, and
it should be treated as blocking alongside GAP-06b.**

### Deferred to V3.2+ — stated with reasons, not just deferred

| Item | Why not V3.1 |
|---|---|
| **AI** (GAP-12, GAP-11) | Largest new domain in the set (own schema, provider abstraction, two-stage authorization, output validation, cost controls, usage limits). Blocked on the hosting/reachability decision. Adds no value until the core loop is operable. Building it before Reviews and the professional surface would be building the demo before the product. |
| **Referral** (GAP-28) | Qualification triggers on completed bookings and rewards through loyalty. Both exist, but a referral loop over a marketplace whose supply side cannot operate has nothing to amplify. `pointsReferralQualified` is deliberately 0 pending a business number. |
| **Wishlist** | Genuinely small, genuinely low-impact until there is imagery worth saving. Pairs naturally with C in a later pass. |
| **CRM** (GAP-16) | V2's implementation is explicitly not portable (unbatched in-memory search/filter/pagination). Needs the professional surface (B) to have somewhere to live. |
| **B2B quotes** (GAP-19) | No quote domain, no B2B tier pricing, no evidence B2B is a real V3 requirement. Should be answered as a business question before any engineering. |
| **Campaigns** (R31-07) | Same — a business question first. The pricing chain accepts it additively whenever the answer arrives. |
| **Automated payout** (GAP-18) | Settlement exists, is append-only and idempotent, so payout is additive within it. Blocked on real banking rails, which are downstream of GAP-06b. |
| **Calendar sync** (GAP-25) | New scope, not a gap being closed. |
| **Kafka / Redis / ClickHouse** | ADR-022 deferred all three on evidence. Nothing in V3.1 changes the evidence. Redis specifically returns in F if the hosting decision produces multiple instances. |

---

## 5. Reconciliation totals

| Status | Count |
|---|---|
| RESOLVED (re-verified this pass) | 30 register/QA items, plus 17 `v3.0.1` fixes |
| OPEN — actionable in V3.1 | 31 |
| OPEN — deferred to V3.2+ with reasons | 9 |
| OPEN / EXTERNAL — externally gated | 4 (GAP-06b, HOSTING_GRANTS, GAP-11, PHASE4-03) |
| BUSINESS DECISION — blocked on a human choice | 7 (GAP-10, GAP-21, HOSTING, QA-17, QA-23, R31-04, R31-07) |
| SUPERSEDED by re-verification | 2 (GAP-04, GAP-19) |
| INFORMATIONAL — no action implied | 5 (GAP-26, GAP-27, QA-22, QA-26, R31-05/06) |
| **NEW this pass** | **7** (R31-01 … R31-07) |

**No BLOCKER-severity defect was found in `v3.0.1`.** Nothing in this reconciliation
invalidates `v3.0.0`, `v3.0.1`, or `EXC-001` as decisions. What it finds is that the
product is **backend-complete and frontend-partial**, with three structurally inert trust
signals and no operable administrative path — and that the two most consequential
unresolved items are not code at all: the hosting decision and the legal/content
deliverables.

---

---

## 7. Task 1 addendum (2026-08-24) — professional operating surface delivered

`V3.1-B`'s first task is implemented. Full report: `V3.1_TASK1_IMPLEMENTATION.md`. This
section records only what changes **this** document.

### Closed

**`PRO-UI` — CLOSED.** All nine professional capabilities that had an API and no frontend
now have one, across seven `/pro` routes. Verified by the complete live browser trace
(profile → service → availability → customer booking → payment → completion → earnings →
history). No backend capability was recreated; three thin routes were added over existing,
already-tested service methods (`GET /v1/me/provider`, `PATCH`/`DELETE` on a service, and
the `GET /v1/cities` + `GET /v1/specialties` reference pair `V3_DOMAIN_BOUNDARIES.md` had
specified and nobody had built).

**`QA-25` — reclassified, not closed.** The professional-mode nav link is shown to every
authenticated user *deliberately*: becoming a professional in V3 is self-service, so `/pro`
is the entry point to a mode anyone may enter. That is not the same situation as the
business link, which is shown to everyone because **no signal exists** to condition it on.
`QA-25` itself is unchanged and still waits on Phase A.

**`TOUCH-CLASS` — partially closed.** `TextLink` now carries the 44px baseline as an
inheritable property rather than a lesson each surface re-learns. The seven new routes were
measured at 375/412/1280 with **zero** sub-44px interactive targets. The class is not fully
closed until the existing customer surfaces adopt the primitive.

**`UIKIT` — materially advanced.** `TextLink`, `PageHeader`, `EmptyState`, `Badge`,
`Select`, `Textarea`, and the codebase's **first** `ConfirmDialog` (with a real focus trap,
which had no precedent anywhere in V3) now exist. Still open for the customer surfaces.

### New findings

**`R31-08` — CORS blocked `Idempotency-Key`, so browser checkout has never worked.
PRE-EXISTING / HIGH / FIXED.** `POST /v1/bookings` requires the header and the web app sends
it, but `main.ts`'s CORS allow-list never named it, so every cross-origin booking was
rejected at **preflight** and the POST was never issued. Customer checkout therefore failed
in every real browser in the deployed topology (API and web on different origins). Invisible
to all 407 API tests, because supertest sets headers directly and no preflight exists. This
is a second instance of the exact class Phase 1 recorded when CORS was found entirely
unconfigured — same file, same reason. Fixed; the fix was strictly required to run Task 1's
mandated end-to-end trace.

**`R31-09` — `persian-utils`' date helpers read host-local time, not the platform zone.
OPEN / MEDIUM.** `formatShortDate`, `formatFullJalaliDate`, and `formatTime` resolve a
`Date` through `getHours()`/`getDate()`, which read whatever zone the browser is in, while
booking-service materializes every slot from a wall clock in `Asia/Tehran`. Correct for a
user in Iran on a machine set to Iran; silently an hour or a day wrong otherwise. Task 1
added an additive, zone-explicit module (`zoned.ts`) and used it throughout `/pro`, leaving
every existing customer call site untouched — changing their behaviour was outside the task.
**The customer surfaces remain latently inconsistent.** Recommended phase: G, alongside the
component-layer work.

**`R31-10` — service update leaked existence as a 500. INTRODUCED AND FIXED IN TASK 1.**
`ServiceOfferingService.update()`'s `findOneOrFail` was harmless while the method had no HTTP
route; exposing it turned TypeORM's `EntityNotFoundError` into a 500, distinguishable from
the response a well-formed nonexistent id gets. Recorded because the *pattern* generalises:
**every service method that gains its first HTTP route needs its not-found path re-examined
at the boundary**, and Phase A will expose several.

### Evidence position — materially changed

**The authenticated application ran locally, in a real browser, for the first time in V3's
history.** Every phase report and both release gates recorded this as impossible in this
environment. It was achieved by provisioning a PostgreSQL 16 container using the project's
own CI provisioning steps and `database/scripts/financial-roles.sql` — no credential
guessing, no `pg_hba.conf` edit.

Consequences for items this document tracks:

- The full real-PostgreSQL suite now runs **locally** as well as in CI: **20 suites / 407
  tests / 0 skipped**, up from 378.
- Three of the four unverified accessibility dimensions in `V3.1_UIUX_BACKLOG.md` §2.7
  (items 21 and 23 — keyboard traversal and populated protected-route layouts) are now
  *reachable*, though only item 23 was exercised in this pass.
- **`HOSTING_GRANTS` is unchanged.** A local container is not a real target hosting
  provider, and this pass makes no claim that it is.

---

## 8. Phase A addendum (2026-08-27) — operability foundation delivered

Full report: `V3.1_PHASE_A_IMPLEMENTATION.md`. This section records only what changes
**this** document.

### Closed

**`R31-01` — CLOSED.** `identity.roles` / `capabilities` / `role_capabilities` /
`user_roles` exist, seeded from `CAPABILITIES_BY_ROLE` so the constant becomes the seed's
source rather than a second authority. Account creation writes the user row and its
default role grant in one transaction. A documented, single-use, audited bootstrap
produces the first `platform_operator`. **Verified live: all five admin API surfaces are
now reachable by a real account, and were refused for a customer on every one.**

**`R31-02` — CLOSED.** `provider.verification_requests` adds the queue the state machine
never had; `VerificationService` calls the existing `transitionVerification()` without
redesigning it. **Verified live: a professional went `pending → verified` through the
admin UI, `badges: ["verified"]` was awarded for the first time in V3's history, the
`verifiedOnly` filter returned the provider, and the search projection updated through
the existing consumer.**

**`GAP-02-V3` — CLOSED.** `admin.admin_audit_log`, owned by a role the application never
connects as, application granted INSERT + SELECT only. UPDATE and DELETE both denied by
PostgreSQL, proven directly. Audit rows commit inside the mutation's own transaction for
the three Phase A actions; the five pre-existing admin mutations that physically cannot
(separate DataSource, or an external system) **declare** that with a required reason, and
a boot-time assertion refuses to start if any privileged mutation declares nothing.

**`GAP-20` — CLOSED.** `resolved_at` was write-never since Phase 1. Resolution is
idempotent by compare-and-swap and writes exactly one audit row. It records that a human
looked; it does not merge accounts, which would be the silent merge
`V3_SECURITY_MODEL.md` §1 forbids with a slower trigger.

**`ADMIN-UI` — CLOSED.** Eight screens as a route group in `apps/web`.

**`R31-05` — CLOSED.** `identity.roles`, `capabilities`, and `user_roles` now exist.
`sessions` remains absent (`QA-20` still needs a JWT claim) and
`business_account_approvals` remains deliberately unnecessary under ADR-023.

**Search quality metrics — DONE, smaller than planned.** Zero-result rate **already
existed** as `emptyResultRate` in `platformMetrics` since Phase 3; rebuilding it would
have been the duplication the brief forbids. Only click-through was added, from event
dimensions that already existed. No raw query text is involved anywhere.

### New finding

**`R31-11` — four design-token pairs are below WCAG AA for normal text. OPEN / MEDIUM /
Phase G.** Contrast was measured for the first time (the UI/UX audit's most significant
unverified dimension): `error`/`error-soft` **4.36**, `warning`/`warning-soft` **4.29**,
`ink-faint`/`surface` **3.98**, `ink-faint`/`surface-tint` **3.58** — all used at 12–14px,
so the large-text allowance does not apply. Eight other pairs pass, several comfortably
(ink/surface 18.09). **None is a Phase A regression**; every one predates this phase.
Deferred rather than fixed because a token change affects every customer screen and
belongs to the phase that owns the component layer.

### Deliberate deviations, recorded

- **`platform_operator` gained `bc_moderate_verification`**, which the Phase 1 static map
  did not give it. Required by Phase A's brief; the alternative (granting the bootstrap
  account `moderator` too) would hand the first privileged account authority over reviews,
  a domain that does not exist. The narrower option, per §9.
- **`RoleGranted` / `RoleRevoked` event contracts were NOT built**, though
  `V3.1_PRODUCT_ROADMAP.md` §15 anticipated them. The audit log already records every
  grant transactionally with actor, before, after and reason; domain events would be a
  second, weaker record of the same facts for a consumer with no current question. Additive
  later if a real need appears.
- **The escalation rule changed during implementation**, from "may only grant a role you
  hold" to "may only grant a role whose capabilities are a subset of your own". The first
  deadlocked — `moderator` would have been permanently ungrantable by anyone, the same
  class of bug as `R31-01`. Caught by the test suite.

### Carried forward unchanged

`GAP-06b`, `HOSTING_GRANTS`, `PHASE4-03`, `HOSTING`, `GAP-10` (now *visible* through an
admin screen rather than only an endpoint nobody could call), `GAP-11`, `R31-03`,
`R31-04`, `R31-09`, `GAP-21`, `GAP-22`, `QA-17`, `QA-23`, `REVIEWS`, `QA-18`, and every
V3.2+ deferral. `GAP-24` (ranking-weight admin) remains deliberately out of the minimum
operational set.

## Cross-references
- `V3.1_PRODUCT_ROADMAP.md` — phases, dependency graph, prioritization, risks.
- `V3.1_UIUX_BACKLOG.md` — the UI/UX items above, classified Bug / Polish / Product Feature.
- `V3.1_RELEASE_STRATEGY.md` — release shape and the `R31-04` exception question.
- `docs/roadmap/v3/V3_GAP_REGISTER.md` — the historical register; its V3.1 addendum points here.


---

## 9. Phase B + G addendum (2026-08-27) — professional surface completion and the component layer

Full report: `V3.1_PHASE_B_IMPLEMENTATION.md`. This section records only what changes
**this** document.

### Closed

**`R31-09` — CLOSED, and it was NOT latent.** This document recorded the host-local date
helpers as a latent inconsistency confined to the customer frontend. It was live and
server-side: `apps/api/src/events/notification-analytics.handlers.ts` builds the `date` and
`time` variables of the booking-confirmed, booking-cancelled and booking-rescheduled
notifications with `formatFullJalaliDate` and `formatTime`, in the API process, where
**nothing sets `TZ`** — not `apps/api/.env`, not the bootstrap, not the CI workflow. On the
ordinary UTC host a container runs on, every customer has been told an appointment time
three and a half hours earlier than their real appointment, and the wrong **day** for any
appointment before 03:30 Tehran.

Fixed centrally: the three helpers delegate to `zoned.ts`, so the platform zone is the
default rather than an opt-in and no call site changed — the defect was reachable from any
call site, including ones not yet written. `toPersianDigits`/`normalizeDigits` moved to a
leaf `digits.ts` to break the resulting import cycle.

**Severity reclassified MEDIUM → HIGH**, on the same reasoning as `R31-08`: a defect that
is invisible to every test and only appears in the deployed topology is not a smaller
defect for being invisible.

**`R31-11` — CLOSED, and it was five pairs, not four.** `ink-faint` on `background` measured
**3.81**; Phase A measured `ink-faint` against `surface` and `surface-tint` but not against
the page background, where it also renders. All 25 pairs the product renders text in now
pass WCAG AA for normal text, **worst 4.66**, measured in a real browser by the same
canvas-readback method and independently asserted by 71 test cases computed from
`tokens.json` itself.

Corrections were central — `ink-faint`, `warning` and `error` darkened with hue preserved,
`error-soft` re-authored into gamut. **No component took a local colour override.**

### New findings

**`R31-12` — two design tokens were outside the sRGB gamut. PRE-EXISTING / MEDIUM /
FIXED.** `warning` (`oklch(0.55 0.13 70)`) and `error-soft` (`oklch(0.95 0.05 25)`) were
clipped by the browser, so the authored colour and the rendered colour were different
colours and any ratio computed from the authored value described something nobody sees.
Nothing had ever checked. `warning`'s AA correction brings it into gamut as a side effect;
`error-soft` is re-authored as the nearest in-gamut colour, rendering `rgb(254,226,222)`
against the clipped original's `rgb(255,226,222)` — a one-unit difference in the red
channel. A test now asserts every token is in gamut.

**`R31-13` — four irreversible actions had no confirmation. PRE-EXISTING / MEDIUM /
FIXED.** Cancelling a booking (the most consequential action a customer can take: it
releases the slot and, on a paid booking, starts a refund), removing a staff member,
declining an invitation, and leaving a business. All four were a single click. All four now
route through the existing `ConfirmDialog` rather than a second modal implementation.

**`R31-14` — TOUCH-CLASS instance six. PRE-EXISTING / MEDIUM / FIXED.** `/bookings`'
empty-state link was a bare `<Link style={{ fontWeight: 600 }}>`, roughly 29px tall; and
`/business`' role radios were bare inputs in labels with no `minHeight`, around 20px
tappable. The class is now **asserted in a test** for every interactive primitive rather
than fixed per instance, which is the first time that has been true.

**`R31-15` — the platform-timezone conversion had four independent implementations.
PRE-EXISTING / LOW / PARTLY FIXED.** `zoned.ts`, booking-service's `platform-time.ts`, and
**two** in `apps/web/lib/booking-api.ts` (`slotTimeLabel`, `groupSlotsByDay`), each building
its own `Intl.DateTimeFormat` with the zone spelled out inline rather than reading
`PLATFORM_TIMEZONE`. The two in `booking-api.ts` now delegate. **Two remain by design** —
the frontend package and the backend service are separate deployables — but they are two
implementations of one rule kept in agreement only by a comment in each.

**`R31-16` — three test defects, each of which had been passing. PRE-EXISTING / FIXED.**
(a) `zoned.spec.ts` asserted that `formatTime` must *disagree* with `formatZonedTime` off
Tehran — it encoded the `R31-09` defect as a requirement, which is why closing that bug
required rewriting the test rather than re-running it; the replacement is strictly stronger.
(b) `format.spec.ts` and `rtl-persian.spec.tsx` built date fixtures with the local-time
`new Date(y, m, d)` constructor, so they passed here (`Asia/Tehran`) and would have passed
in CI (UTC) **by luck**, while failing on any host east of Iran. (c) `pro-surface.spec.tsx`
answered a `POST /services` with the list shape, which produced a real React key warning on
every run and concealed a genuine unchecked-cast defect in the product. The suite is now
verified green under four timezones; it had only ever run in one.

### Re-classified

**`QA-25` — NOT A BUG as stated.** The business nav link showing to every authenticated
user was recorded as a bug blocked on a role signal, expected to fall out of Phase A.
`POST /v1/businesses` is fully self-service, exactly as `POST /v1/providers` is, so
`/business` is an entry point to a mode anyone may enter — the same reasoning `app-shell.tsx`
already records for the `/pro` link. Phase A did make a `business` role grantable, but
nothing grants it on business creation, and adding that would mean business-service writing
to identity's tables to condition a nav link. Addressed instead as the landing problem it
is. Returns as written if business creation ever stops being self-service.

### Unchanged, and blocked

**The authenticated live-browser trace could not be run.** Docker's engine will not start on
this machine (the `docker-desktop` WSL distribution returns to `Stopped` immediately and
`docker info` never succeeds), and the local PostgreSQL 16 service's superuser password is
unavailable with no `pgpass.conf`; `pg_hba.conf` was deliberately not edited. Task 1's
container, which both it and Phase A used, no longer exists.

Consequences for items this document tracks:

- Backlog items **21** (keyboard traversal) and **23** (populated protected-route layouts)
  are **partly closed only** — forward keyboard traversal was walked and is clean, but on
  **unauthenticated** routes. No authenticated screen was reached in this pass.
- `HOSTING_GRANTS` is unchanged, as before.
- **`R31-04` is untouched.** No `v3.1.0` tag, no release, no exception proposed or approved.


---

## 10. Final QA addendum (2026-08-27) — live verification pass

Full report: `V3.1_PHASE_B_G_FINAL_QA.md`. No new finding changes this document's gap
classifications; this section records only the verification status of the fixes it tracks.

**`R31-08` (CORS) — CONFIRMED live.** `OPTIONS /v1/bookings` preflight from the web origin
returns 204 with `Idempotency-Key` in `Access-Control-Allow-Headers`. The full cross-origin
booking POST needs an authenticated session and was not run.

**`R31-09` (timezone) — CONFIRMED live and reclassification upheld.** An instant at 21:00 UTC
renders as the Tehran wall clock (00:30, next day) identically under `TZ=UTC` and the host
zone — the helper does not read host time. The server-side notification handler delegates to
`zoned.ts`; `notification-delivery.pg-spec` passed in the local 448-suite, which is green
under four zones.

**`R31-11` / `R31-12` (contrast + gamut) — CONFIRMED on the live build.** Every corrected pair
measures >=4.5 in the running browser (worst 4.66), and the served tokens are the corrected,
in-gamut values.

**`R31-13` / `R31-14` (confirmations, touch targets) — component-tested; browser confirmation
of the authenticated surfaces remains blocked.** The `/bookings` empty-state touch target and
both destructive-action dialogs are asserted in `customer-surface.spec.tsx`.

**Runtime and authentication.** The Docker runtime was recovered this pass (stale-socket
crash, fixed without weakening security). The **authenticated browser trace remains blocked**
because the environment's safety classifier refuses every route to a login OTP — recovering,
setting, or observing it. The real-PostgreSQL suite (448/448) passed **locally**, covering the
logic of the blocked steps, but the task forbids treating DB tests as browser evidence.

**`R31-04` untouched.** No `v3.1.0` tag, no release, no exception.


---

## 11. Authenticated browser QA addendum (2026-08-27) — two browser-only bugs

Full evidence: `V3.1_PHASE_B_G_FINAL_QA.md` §21-22. The dev-QA-login (committed `2a56ea9`,
CI-green, disabled by default) was activated in the disposable dev runtime and the full
authenticated Definition-of-Done was run in a real browser. Two browser-only bugs surfaced —
both invisible to the 448-suite, the same class as `R31-08` (CORS).

**`R31-17` — browser sandbox payment never verifies. PRE-EXISTING / HIGH / RELEASE-BLOCKING /
OPEN.** `checkout.controller.ts` `callbackUrl()` hardcodes `/v1/payments/callback/mock`, but the
sandbox provider key is `sandbox`. The browser's post-payment redirect therefore runs
`handleCallback('mock', ...)`, which cannot find the `sandbox`-keyed attempt → the payment is
never marked succeeded and the booking stays pending. Verified: `/callback/sandbox` verifies
the same reference correctly. The payment pg-specs miss it because they call `handleCallback`
with the correct provider and never exercise `callbackUrl()`. **This blocks the customer
payment loop in the browser and is the reason the release status is BLOCKED.** A fix task is
spawned; fix = use the intent's real provider key, plus a regression test over the real
callback-URL path.

**`R31-18` — DELETE shows a false "invalid server response". PRE-EXISTING / MEDIUM / OPEN.**
`api-client.ts` calls `response.json()` unconditionally; DELETE routes return 204 No Content, so
the empty-body parse throws and the UI shows an error and keeps the stale row even though the
server deleted it. Affects service delete and slot release. Fix = treat 204/empty body as a
successful null response.

Everything else in the authenticated trace passed: profile create/edit, service create/edit,
availability single + bulk (idempotency), booking + the R31-08 CORS path, the completion fan-out
(loyalty 0→10, journey 2→3, completed_bookings 0→1, earnings 255,000, analytics 100%), admin
verification approve → `verified` + audit + search projection, the full authorization matrix
(403/404/401), R31-09 timezone (14:00 Tehran stored as 10:30 UTC, notification shows 14:00
Tehran Jalali), and logout (200 → refresh 401).


---

## 12. R31-17 / R31-18 — RESOLVED (2026-08-27) with a targeted browser regression

Both browser-only bugs from §11 are fixed, verified against real PostgreSQL and end to end
in a real browser.

**`R31-17` — RESOLVED.** `PaymentService.initiate` now takes a callback BASE and appends the
intent's OWN `provider.key`, so the gateway's return leg always addresses the provider that
will verify it; the checkout controller's create and reinitiate paths both pass the base. The
hardcoded `/callback/mock` is gone. §3 hardened: `prepareVerification` reads
`attempt.providerKey` (trusted server state) for the verify provider rather than the callback
path parameter. No architecture change, no new abstraction, no provider-interface change.

- Regression (`sandbox-payment-lifecycle.pg-spec`): the redirect now embeds `/callback/sandbox`
  and not `/callback/mock`; `handleCallback('mock', ref)` cannot verify a sandbox payment.
- Real PostgreSQL: **450/450, 0 skipped** (payment-security §3/§5/§6, financial-integrity,
  sandbox lifecycle all green).
- Browser: booking → sandbox pay → the return leg now verifies; **order = paid, booking =
  confirmed** (was stuck pending), ledger commission 37,500 + receivable 212,500, once. The
  customer's bookings screen shows «تأیید شده».

**`R31-18` — RESOLVED.** `api-client.ts` short-circuits `status === 204` to a successful null
envelope before parsing, so a body-less No Content (every DELETE route) is a success, not a
parse error. JSON responses (200/201 envelopes, 4xx/5xx errors) are unchanged.

- Regression (`api-client.spec`): 204 → null success; 200-with-JSON still parses; 4xx/5xx
  still surface.
- Browser: deleting a service now removes the card immediately with **no false error**, and a
  reload confirms the deletion persisted.

Commits `7c742d2` (R31-17), `c2f12bf` (R31-18). **`R31-04` untouched; no `v3.1.0`, no release.**

---

## 13. `R31-04` RESOLVED by `EXC-002` — the last release gate closes (2026-08-28)

`R31-04` was the sole remaining blocker at the `v3.1.0` release gate. Every technical gate
had passed; the release was held on a governance question that this pass was correct not to
answer on its own authority — resolution was, as §4 recorded, *"a decision, not engineering."*

**The decision.** The project owner selected **Option A** of `V3.1_RELEASE_STRATEGY.md` §5
by explicit written direction dated 2026-08-28: record a new exception, do **not** amend
`EXC-001`, and do **not** silently reinterpret it.

**`EXC-002` — V3.0.x / V3.1.x Payment Sandbox Release Exception**
(`docs/roadmap/v3/V3_RELEASE_POLICY_EXCEPTIONS.md`), ACTIVE, decided 2026-08-28, scoped to
*"every release in the `v3.0.x` and `v3.1.x` lines while GAP-06b remains open"*,
retrospectively covering `v3.0.1` and covering `v3.1.0`. It carries the full apparatus
`EXC-001` does — unmet criterion, acceptability reasons, accepted risks, safeguards,
production-activation requirements, review conditions, owner — plus two things `EXC-001`
did not need: an explicit statement of what the exception does **not** mean, and a **dated
retirement condition**.

**Re-verified, not copied.** Every one of the fifteen acceptability reasons and ten
safeguards in `EXC-002` was re-checked against the `v3.1.0` release candidate at this gate,
with its evidence class named (`code` / `CI` / `DB` / `browser`). Two are new since
`EXC-001` and could not have been copied from it:

- **Trusted-server-state provider resolution** (`R31-17` §3). `prepareVerification` reads
  `attempt.providerKey`, never the callback's path parameter — proven adversarially in a
  real browser this pass: `/v1/payments/callback/mock` carrying a genuine `sandbox`
  reference returns **404** and verifies nothing.
- **Currency validated rather than assumed.** The intent's currency is compared against the
  provider-reported `paidCurrency`; the sandbox declares `IRT` explicitly.

**`EXC-001` untouched.** Byte-identical to its `v3.0.0`-era text, verified by diff at this
gate. Amending a published exception's scope after the fact would have rewritten the record
of what was decided and made its scope clause retrospectively meaningless — the exact drift
that produced `R31-04`. `EXC-002` states its retrospective coverage of `v3.0.1` in its own
text; no historical release document or tag object was rewritten to imply it existed then.

**`GAP-06b` is NOT closed by this.** Re-verified OPEN / EXTERNAL_CONFIGURATION at this gate
from source: sandbox is the only registered provider, no adapter, no merchant credential,
`PAYMENT_ALLOW_MOCK_GATEWAY` absent from tracked source, and the two-condition no-override
production gate intact — production resolves zero payment providers and checkout fails
closed. `EXC-002` waives a release criterion for a **tag**, never for production payment
enablement.

**Retirement.** `EXC-002` retires at `v3.2.0`, or earlier if `GAP-06b` is genuinely closed
first. If `GAP-06b` is still open at `v3.2.0` the exception **does not silently extend** — a
new explicit governance decision, with its own re-verification, is required. Letting it lapse
by convention is the failure mode that produced `R31-04` in the first place.

### Status

| Item | Before | Now |
|---|---|---|
| `R31-04` | OPEN / BUSINESS DECISION | **RESOLVED** by `EXC-002` |
| `GAP-06b` | OPEN / EXTERNAL_CONFIGURATION | **OPEN / EXTERNAL_CONFIGURATION** (unchanged) |
| `EXC-001` | ACTIVE, `v3.0.0` only | ACTIVE, `v3.0.0` only — **unchanged** |
| `EXC-002` | — | **ACTIVE**, `v3.0.x` + `v3.1.x` while `GAP-06b` open |
| `v3.1.0` release gate | BLOCKED on `R31-04` | **all gates PASS** |

### One new finding at this gate

**`R31-19` — `GET /v1/providers/{non-uuid}` returns 500 rather than 400/404. NEW / LOW /
PRE-EXISTING / NOT RELEASE-BLOCKING.** No leak (generic Persian error, no stack), no
security or integrity impact. Present since the original V3 provider foundation and
therefore in `v3.0.0` and `v3.0.1` — not a V3.1 regression. Full entry in
`V3_GAP_REGISTER.md`.

---

## 14. `R31-20` — the release audit's own browser re-run found one more blocker (2026-08-28)

`V3.1_RELEASE_STRATEGY.md` §7 requires *"live browser QA performed and its limitations
stated"*. Performing it hands-off — rather than helping the flow along — found a
release-blocking defect that had survived `R31-17`'s fix, that whole sign-off, and this
audit's own first browser run.

**`R31-20` — the sandbox gateway never returned the browser to the callback. HIGH /
RELEASE-BLOCKING / PRE-EXISTING — FIXED.** The gateway page read `accepted` off the response
**envelope** instead of `envelope.data`, so it was always `undefined` and the refusal guard
fired on every response, successful ones included. The decision was recorded as `paid`, the
customer saw a false «این تراکنش پیش‌تر نهایی شده است», and the browser was never returned
to `/callback/sandbox` — order `pending` forever. Full entry in `V3_GAP_REGISTER.md`.

**Why the earlier sign-off missed it, stated plainly.** §21 and §23 above record the browser
payment flow as passing. That evidence was real but **assisted**: in both the previous QA
pass and this audit's first run, the return leg was completed by navigating to the callback
URL by hand, and the resulting `paid` order was read as proof the loop worked. It proved the
callback *worked*; it could not prove the gateway page *used* it. The corrected claim is that
before this fix, the browser payment loop never completed unaided in any run, at any point in
V3's history.

That is a lesson about method, not about any one bug: **a manual step inserted to get past
an obstacle silently becomes part of the evidence.** `R31-17`, `R31-18`, and `R31-20` are all
browser-only, all invisible to the API suites, and `R31-20` was additionally invisible to
the browser QA that helped it along.

**Fix + regression:** `envelope.data` is read rather than the envelope, and
`apps/web/test/sandbox-gateway-page.spec.tsx` (5 cases) pins both halves — the success path
must navigate to the callback carrying only the reference, and each genuine refusal must
still refuse without navigating. Verified to FAIL (3 of 5) with the fix reverted, so it
cannot silently stop testing anything.

**Re-verified hands-off in a real browser** against the rebuilt production bundle: one click
on «پرداخت موفق», no manual navigation → `/callback/sandbox` → «پرداخت انجام شد / رزرو شما
تأیید شد», order `paid`, booking `confirmed`, ledger commission 45,000 + receivable 255,000
exactly once; replay → `status=replayed` with the ledger unchanged; `/callback/mock` with the
genuine `sandbox` reference → 404.

### Test gate after the fix

Fast suite **534/534, 0 skipped** (web 130, +5 new `R31-20` cases); real PostgreSQL +
OpenSearch **450/450, 0 skipped**; typecheck (25 projects), lint (0/0), and a fresh
`--skip-nx-cache` production build all green.

### One further finding, not release-blocking

**`R31-19` — `GET /v1/providers/{non-uuid}` returns 500 rather than 400/404. NEW / LOW /
PRE-EXISTING.** No leak, no security or integrity impact, present since the original V3
provider foundation and therefore in `v3.0.0` and `v3.0.1` — not a V3.1 regression. Full
entry in `V3_GAP_REGISTER.md`.

---

## 15. Phase E addendum (2026-08-28) — privacy, auth polish, and the SMS provider port

**Baseline for this pass:** `432647b` = `origin/master`, CI-green.
Full report: `V3.1_PHASE_E_IMPLEMENTATION.md`. Architecture: `ADR-027-subject-data-contract.md`.

### Register changes

| ID | Was | Now | Evidence |
|---|---|---|---|
| **GAP-22** | OPEN · HIGH / LEGAL | **RESOLVED** | `privacy` schema, four self-service routes, one admin read route, sixteen per-module contracts, 31 real-PostgreSQL cases |
| **GAP-21** | OPEN · BUSINESS DECISION | **RESOLVED (mechanism) · BUSINESS DECISION VISIBLE (number)** | `execute_after` grace window; seven-day default is `PRIVACY_ERASURE_GRACE_HOURS` and remains a `GAP-10` figure. A zero or unparseable value falls back to the default rather than propagating |
| **QA-19** | OPEN · LOW | **RESOLVED (API) · UI REQUIRED** | `cooldownRemaining`, `expiresInSeconds`, and `retryAfterSeconds` on refusal; 4 cases including the anti-enumeration check |
| **QA-20** | OPEN · BUG · LOW | **RESOLVED** | `sid` claim, optional; `current` is real; 6 cases |
| **GAP-11** | OPEN / EXTERNAL | **ADVANCED — still OPEN / EXTERNAL** | Reclassified from "no abstraction exercised" to "abstraction complete and exercised against a real HTTP server; awaiting vendor selection and one live send" |
| **QA-17**, **QA-23**, **QA-16r**, **GAP-09** | OPEN | **UNCHANGED** | Blocked on decisions #14/#15 and on UI freeze — see below |

### Three defects found and fixed during the phase

- **`BUG-E-01`** — a `@Global()` service could not resolve a token provided by the composition
  root that imports it, and `@Optional()` turned the misresolution into an empty array. The
  boot check then ran against zero contracts and reported every table in the database as
  unclaimed. Fixed twice over: the contract list became a method parameter, and the
  composition module became `@Global()`. **The third instance of this DI trap in the project**
  (Phase 4's `PRICING_RULES`, Phase C's `BUG-C-02`), and the first one whose symptom was a
  silently *empty* result rather than a boot failure — with only the first half of the fix
  applied, exports contained zero sections and erasure erased nothing, both of which look like
  working code.
- **`BUG-E-02`** — `GET /v1/auth/sessions` returned one row per token ROTATION rather than one
  per device, so a phone used daily contributed several hundred indistinguishable rows, and
  `createdAt` reported when the current token was minted rather than when the device signed
  in. Pre-existing, not introduced by this phase, and fixed with `QA-20` because it made
  `QA-20`'s feature unusable regardless.
- **`BUG-E-03`** — the real-PostgreSQL harness's `envOverrides` never reached `ConfigService`.
  `AppModule` was imported statically at the top of `pg-test-app.factory.ts`, so
  `ConfigModule.forRoot({ validate })` snapshotted `process.env` at **file load**, before
  `createPgTestApp` had assigned anything. Overrides therefore affected only values read
  straight from `process.env` (the throttler options) and never values read through
  `ConfigService` — every OTP, booking, loyalty, and privacy policy number. It surfaced as a
  suite that **passed under bare `jest` and failed under `nx run api:test:pg`**, because only
  the latter loads `apps/api/.env` into `process.env` first. Test-harness only; no production
  path constructs `AppModule` after mutating the environment. Fixed by importing `AppModule`
  dynamically, after the environment is applied.

### Deliberate deviations, recorded

- **A second privacy table.** The roadmap specifies `privacy.data_requests` alone, preserving
  V2's shape. `privacy.export_payloads` was added because the request row is administratively
  visible and the payload must be readable by exactly one principal — separating them makes
  "no admin route may ever download another user's export" structural rather than a `SELECT`
  somebody must remember not to write. Argued in ADR-027 §6.
- **The export is stored in PostgreSQL, not object storage.** Phase C's `ObjectStorageDriver`
  exists and was not used: a presigned URL to a complete personal-data export is a bearer
  credential that survives being forwarded and proxy-logged, where an authenticated route
  re-verifies the subject's session on every byte.
- **An additive enum widening on five v1 contracts.** `NOTIFICATION_CATEGORIES` gains
  `privacy`. Argued rather than assumed — no field changes type, none is removed, and every
  value an existing consumer understood still arrives; a member *removed* would be the
  breaking change and would deserve a version. Recorded in the catalogue file itself.
- **`DataErasureCompleted` has no notification consumer.** By the time it publishes the
  subject has no phone number and no session; the message that matters goes out on the
  *request*, while the window is open.
- **`analytics` records the privacy events with no subject at all.** `actorOf` is absent from
  all five mappings, so the allow-listed dimensions shape means the id never reaches the fact
  table. An analytics store permanently recording who asked to be forgotten would be an
  unusually pointed thing to get wrong.

### Carried forward unchanged

`GAP-06b`, `HOSTING_GRANTS`, `PHASE4-03`, `HOSTING`, `GAP-10`, `R31-04`(closed by `EXC-002`),
`R31-09`, `R31-19`, `QA-17`, `QA-23`, `QA-16r`, `GAP-09`, `QA-21`, `QA-22`, `QA-25`,
`THROTTLE-STORE`, `UIKIT`/`A11Y-CONTRAST`/`TOUCH-CLASS`, and every V3.2+ deferral.

### One new business decision, deliberately not made

**What happens to a business whose owner erases their personal account?** Close it, leave it
unassigned, or transfer ownership to a manager — three defensible answers with real
consequences for staff and for customers holding bookings. Until it is decided,
`business.businesses` is retained and the erased owner's staff membership is set to `removed`.
Recorded in `business-subject-data.contract.ts` and in the phase report §14.

---

## 16. V3.2+ planning addendum — internal chat restored and deferred scope ordered (2026-08-29)

The owner directed that the former V3.2+ deferral bucket become an ordered roadmap and that
**internal chat**, present in the original product direction and the historical migration
plan but absent from both V3 code and this reconciliation's deferred table, be restored.

`docs/roadmap/v3.2/V3.2_PRODUCT_ROADMAP.md` now records:

- V3.2 core: AI Assistant, Internal Chat, Referral, Wishlist;
- professional growth: CRM and independent-professional delegation;
- decision-gated commercial expansion: B2B quotes and campaigns;
- external-rail automation: payout and calendar sync;
- evidence-gated infrastructure and advanced improvements.

**New tracked item: `CHAT` — OPEN / PRODUCT GAP / V3.2+.** No chat service, schema,
controller, route, or frontend exists in V3. Before schema work, product decisions are
required for participants, eligibility, retention/erasure, attachments, blocking/reporting,
moderation/support access, and realtime expectations. Human messages are not AI context by
default.

The §2 master index and §5 totals are retained as the original V3.1 discovery snapshot;
this dated addendum is the authoritative later disposition. No V3.2 implementation or
business decision is implied by adding the item to the roadmap.

---

## 17. V3.2–V3.4 comprehensive capability classification (2026-08-29)

At the owner's direction, the later roadmap now classifies every capability carried beyond
V3.1 instead of leaving smaller items in a generic V3.2+ bucket. The authoritative inventory
is `docs/roadmap/v3.2/V3.2_PLUS_CAPABILITY_CATALOG.md` and uses five actionable classes:
core, product-decision gated, external-provider gated, evidence-gated, and quality/maturity.
A sixth class records retired or prohibited directions so they are not accidentally revived.

The programme bands are now:

- **V3.2:** AI, internal chat, referral/wishlist, CRM/delegation, B2B/campaign decisions,
  payout/calendar integrations, and narrowly evidence-gated improvements;
- **V3.3:** membership/recurring billing, omnichannel retention, business operations,
  discovery/geo/media maturity, trust/privacy/accessibility, advanced waitlist/referral
  analytics, and the admin/analytics control plane;
- **V3.4:** conditional mobile/PWA/rich media, marketplace/accounting expansion, consented
  advanced AI/realtime collaboration, and evidence-gated data/scale infrastructure.

This classification supplies a target and gate for known capabilities; it does not approve
unresolved product policy, select vendors, or turn scale hypotheses into engineering work.


---

## 18. Phase F addendum (2026-08-29) — the non-external half of production enablement

**Baseline for this pass:** `bb844ee` = `origin/master`, CI-green.
Full report: `V3.1_PHASE_F_IMPLEMENTATION.md`. Architecture:
`ADR-028-ambiguous-verification-and-readiness.md`.

Executed under `V3.1_EXTERNAL_ENABLEMENT_STRATEGY.md`, which separates code and
sandbox readiness from production evidence. **Milestone reached:
`IMPLEMENTATION COMPLETE / SANDBOX READY` for the provider-neutral portion of
Phase F. Phase F is not complete and no external gap closed.**

### Register changes

| ID | Was | Now | Evidence |
|---|---|---|---|
| **QA-21** | OPEN · UX · LOW | **RESOLVED** | `failureCode` threaded through the redirect contract as a closed public `reason` vocabulary; cancel and decline are different sentences; 12 pg cases + 8 web cases |
| **HOSTING_GRANTS** | EXTERNAL · EVIDENCE-GATED | **ADVANCED — still EXTERNAL** | `pnpm verify:roles`: 44 assertions, no superuser required, run by CI as a command including its exit codes. Reclassified from "one script, one afternoon, once a host exists" to "one command, tested, awaiting a host" |
| **PHASE4-03** | OPEN · EXTERNAL CONFIGURATION | **ADVANCED — still EXTERNAL** | Now an explicit assertion (`schema.public.app_create`) rather than a lesson in a CI comment. Unverified on any real host |
| **OPS-02** (backups) | Nothing existed | **ADVANCED — still EXTERNAL** | `pnpm backup` + `pnpm restore:rehearse`, run in CI; locally 71 tables, 37 migrations, and 44/44 role checks **on the restored copy**. The drill §9 requires — real data, real host, new cluster — is untouched |
| **OPS-03** (monitoring) | Nothing existed | **ADVANCED — still EXTERNAL** | Prometheus exposition at `GET /metrics`, token-gated, 404 when unconfigured; structured JSON logging with redaction. No scraper, no dashboard |
| **OPS-04** (error tracking) | Nothing existed | **ADVANCED — still EXTERNAL** | `ErrorReporterPort`, a logging default reporting `reportsExternally: false`, and a configurable HTTP reporter verified against a real local server that refuses, errors, and hangs. No backend selected, no DSN, no production error received |
| **THROTTLE-STORE** | OPEN · PERFORMANCE / SECURITY | **UNCHANGED — reclassified TOPOLOGY-DEPENDENT** | Deliberately not implemented: a shared store requires deciding that production runs more than one instance, which is downstream of `HOSTING`. The fact is now *reported* on `/health/ready` instead of being deducible only from a suspiciously generous rate limit |
| **GAP-06b** | OPEN / EXTERNAL · BLOCKING (prod) | **ADVANCED — still OPEN / EXTERNAL** | The caller-side contract is settled: a third `unknown` verification outcome that writes nothing, a caller-imposed deadline, the money-unit boundary, and the public failure vocabulary. No adapter, no credentials, no gateway sandbox, no real-money transaction |
| **HOSTING**, **GAP-11**, **QA-17**, **QA-23**, **R31-04** | OPEN | **UNCHANGED** | All business or external. See `V3.1_PHASE_F_IMPLEMENTATION.md` §11 |

### One re-classification worth stating plainly

Four items had been carried for five phases as small work waiting on a host —
`HOSTING_GRANTS`, `PHASE4-03`, `OPS-02`, and the monitoring pair. Building them
showed that in each case the work waiting on a host was a *script*, not a
decision. Each is now one tested command. **None of that closes anything**: a
command that has never met the target is not evidence about the target, and the
scripts say so in their own output rather than letting a green CI run imply it.

### Six defects found and fixed during the phase

Every one was found by a test or by running the artifact, not by review.

- **`BUG-F-01`** — the failure-code lookup was an object literal, so a gateway code named
  `constructor`, `toString`, or `hasOwnProperty` resolved a FUNCTION off `Object.prototype`
  and returned it as a reason — which `URLSearchParams` stringifies, publishing a chunk of
  the runtime's source into the customer's address bar. Exactly the leak the closed
  vocabulary exists to prevent, arriving through the lookup rather than the table.
- **`BUG-F-02`** — `payment_attempts.failure_code` is `varchar(60)` and PostgreSQL raises
  `22001` rather than truncating. Inside the callback's transaction. So a gateway returning a
  longer message — a free-text Persian bank message, which several Iranian gateways do —
  served the customer a 500 **on the failure path**. Adapter strings are now bounded at the
  boundary; the provider REFERENCE is refused loudly instead, because a truncated one is a
  payment nothing can ever reconcile.
- **`BUG-F-03`** — HTTP metrics as an interceptor made **every 4xx invisible**: Nest runs
  guards before interceptors, so 401, 403, 404, and 429 never reached it, and an unmatched
  route never enters the pipeline at all. The error-rate dashboard would have read zero
  during exactly the incidents it exists to surface. Now middleware.
- **`BUG-F-04`** — that middleware never called `next()`, which does not fail loudly: it
  hangs every request in the application forever, with no error anywhere.
- **`BUG-F-05`** — the new error-reporting hook assumed an HTTP `ArgumentsHost`, so the
  exception filter threw **while handling an exception** — an unhandled rejection and no
  response at all, turning a recoverable 500 into a dropped request. Found by the
  pre-existing filter spec, whose host double implements `getResponse` and nothing else.
- **`BUG-F-06`** — `apps/api/tsconfig.json` redeclared `paths` as a **subset**, twelve of
  twenty-six packages. Everything absent resolved through `node_modules` to each package's
  own `src/index.ts` — fine for `ts-node` and every test runner, and silently broken for
  `tsc -p`, because **TypeScript does not emit files it reached through `node_modules`**.
  `dist/` had been missing `libs/audit`, `libs/media`, `libs/subject-data`,
  `libs/event-contracts`, and `libs/testing` entirely: a green build whose output cannot
  start, for as long as nobody ran it. Found by running the container. The config now
  inherits, which removes the class rather than lengthening the list.

`BUG-F-03` through `BUG-F-06` share a shape worth recording: each was invisible to the test
suite as it stood and became visible the moment the code was *executed in the shape it will
actually run in* — through the real router, in a real container, from compiled output. That
is the same lesson Phase 1 recorded about CORS ("API-only testing cannot surface
browser-enforced policy") and Phase 2 about pg-mem, arriving in a third place.

### What did not change

`GAP-06b`, `HOSTING`, `HOSTING_GRANTS`, `PHASE4-03`, `GAP-11`, `OPS-02`, `OPS-03`, `OPS-04`,
`QA-17`, `QA-23`, `GAP-10`, `R31-04`, and `THROTTLE-STORE` are all still open. The
two-condition, no-override payment sandbox gate is untouched and is now refused twice — the
provider gate stops a sandbox taking real money, and boot-time environment validation stops a
deployment that thinks it can from starting at all.

The full External Enablement blocker ledger — eighteen items, each naming the exact decision,
credential, host, vendor, or live check — is `V3.1_PHASE_F_IMPLEMENTATION.md` §11.


---

## 19. Phase F design-implementation addendum (2026-08-29) — the payment result page

**Baseline for this pass:** `0bb2263` = `origin/master`, CI-green (run
`33229069474`). **Design source:** `origin/design/claude-design` @ `4b40b90`,
`docs/design/phase-f-workspace-snapshot/docs/design/screens/16_CHECKOUT_RESULT.md`.
Full report: `V3.1_PHASE_F_IMPLEMENTATION.md` §§2.2b–2.2d.

### Register changes

| ID | Was | Now | Evidence |
|---|---|---|---|
| **QA-21** | RESOLVED (redirect contract) | **RESOLVED — and now actionable** | The four server-authorized retry reasons have a working CTA. `POST /v1/orders/:id/payment/retry`, 20 real-PostgreSQL cases |
| **UNRESOLVED-COPY** | *not previously tracked* | **RESOLVED · NEW, found by design** | The `unresolved` body promised automatic resolution that no code performs. See below |
| **GAP-06b** | OPEN / EXTERNAL | **UNCHANGED** | Nothing here selects a gateway, writes an adapter, or invents settlement semantics. The retry reuses the existing `initiate` path |
| **EXPIRED-REBOOK** | *not previously tracked* | **OPEN · BACKEND CONTRACT REQUIRED · NEW** | `expired` deliberately gets no retry: the slot may already belong to somebody else, and re-booking is a booking question with no safe path today |

### The finding worth recording

**A claim in customer-facing copy outlived the code it described.** The
`unresolved` result told customers that any deducted amount would be
"به‌صورت خودکار تعیین تکلیف می‌شود". `V3.1_PHASE_F_IMPLEMENTATION.md` §8 records
that no reconciliation sweep was built, and why. The sentence therefore
promised a mechanism that does not exist, to a customer whose money might be in
question, at the moment they are deciding whether to pay again.

`gateway_error` carried the milder form: an automatic-refund promise for a
DEFINITIVE failure, where by definition nothing was captured.

This is the **second time in Phase F** that prose outlived its implementation —
`.env.example` had claimed since Phase 3 that a config validator refused an
invalid cookie combination, and none existed. Both are now true. The pattern is
worth stating: **the copy a customer reads is part of the contract, and nothing
was checking it against the contract.** The new suite asserts the absence of
the promises (`not.toContain('تعیین تکلیف')`, `not.toContain('بازگردانده می‌شود')`)
rather than only the presence of the replacements, so a well-meaning future
edit cannot quietly reintroduce them.

### The retry contract, and why it is safe

Order-scoped rather than intent-scoped: an intent id in a URL is a
payment-domain identifier in browser history, referrer headers, and every
analytics script the page loads. Six checks, every input read from the
database and none from the request. The one that is not obvious:

> An `unknown` verification writes NOTHING (ADR-028), so after
> `declined → retry → paid → gateway times out` the intent still carries
> `failure_code = 'declined'`. A retry decision made on the failure code alone
> would offer to charge that customer a second time for a payment that may
> already have taken their money.

So the command also refuses whenever an attempt is still `initiated` — at the
bank, abandoned, and unresolved are indistinguishable from there and are
refused identically.

`@beauclick/payment-contract` gives the browser and the server ONE definition
of the vocabulary and the retryability rule. Sharing the rule removes drift; it
does not move the decision, and the server refuses regardless of what the page
believed.

### One defect found

**`BUG-F-07`** — `infra/docker/tsconfig.runtime.json` is generated from
`tsconfig.base.json` and nothing regenerates it, so a new path mapping left the
container's runtime resolver without it. Typecheck, lint, every test, and the
image BUILD all passed; the process would have died at startup. Found by
running the container. Same class as `BUG-F-06`, and the second time in this
phase that only executing the artifact in the shape it actually runs in caught
it.

### What did not change

Every external gap. `GAP-06b`, `HOSTING`, `HOSTING_GRANTS`, `GAP-11`, `OPS-02`,
`OPS-03`, `OPS-04`, `PHASE4-03`, `THROTTLE-STORE`, `QA-17`, `QA-23`, `R31-04`.
No gateway selected, no settlement semantics invented, no reconciliation sweep
built, no tag created. The milestone is unchanged:
`IMPLEMENTATION COMPLETE / SANDBOX READY`.

# V3.1 Development-Only QA Login — Design and Status

**Status: IMPLEMENTED, VERIFIED, COMMITTED (`2a56ea9`, CI-green), USED SUCCESSFULLY for the
authenticated browser Definition-of-Done, and now DISABLED BY DEFAULT.**

History, because it matters for trust:

- First "enablement" task (earlier 2026-08-27): written, then **reverted** — the environment's
  safety classifier blocked typechecking/building/testing, so its production-safety guard could
  not be proven and unverified auth-bypass code must not be committed.
- "Unblock" task: the environment permitted build/test but blocked committing and activating.
  The mechanism was reinstated and **fully verified** (production-safety proof executed), but
  not committed.
- This task: committing and activating were permitted. The mechanism was committed (`2a56ea9`,
  CI-green including the production-safety regression test), activated in the disposable dev
  runtime, and **used to run the full authenticated browser trace** (§11 below,
  `V3.1_PHASE_B_G_FINAL_QA.md` §21). It was then disabled again in the runtime `.env`.

The production guarantee is proven, executed, and CI-guarded: `dev-qa-login.spec.ts` asserts the
policy is disabled when `NODE_ENV=production` regardless of the flag/allow-list, and
`auth.e2e-spec.ts` asserts the route returns 404 in production under the same conditions. The
normal OTP flow is unchanged and green.

**Committed as a disabled-by-default, production-impossible, regression-tested, documented dev
tool** — the retain decision (§10). No committed config enables it.

---

## 1. Purpose

The product authenticates by OTP. The QA environment deliberately never exposes the generated
code (`V3_SECURITY_MODEL.md` §2: never log, return, or persist a plaintext code). That is
correct, and it is why the authenticated browser trace cannot run without a sanctioned dev
seam. This is that seam: a development-only route that establishes a **normal** session for a
fixed set of QA phone numbers without an OTP, and is impossible to activate in production.

## 2. Security boundary — the one property everything serves

**When `NODE_ENV === 'production'`, the mechanism is OFF, unconditionally.** `DEV_QA_LOGIN=1`
in production does nothing. The two conditions are AND-ed and the production check cannot be
overridden by any env var, header, or body field. The route responds in production exactly as
if it did not exist (404), so it can neither be probed nor activated.

Defence in depth, all present in the design:

- **Two-condition enable:** `NODE_ENV !== 'production'` **and** `DEV_QA_LOGIN === '1'`. Off by
  default; a forgotten flag means "off", never "on".
- **Positive phone allow-list:** `DEV_QA_LOGIN_PHONES` (comma-separated). With no list, the
  seam authenticates **nobody** even when enabled — it cannot mint arbitrary identities. The
  list is checked in the controller against the raw input and again in the service against the
  **canonical** phone, so a formatting trick cannot smuggle a non-QA number past it.
- **Re-evaluated per request:** the policy is read from the environment on every call, so no
  cached flag can outlive a config change.
- **No new authorization model:** the session is issued by the SAME `TokenService.issuePair` a
  real login uses. `req.user`, every ownership resolver, and every capability guard operate on
  it identically and cannot tell it apart. Only `otp.verifyOtp` is skipped — the single step
  that cannot run in this environment.
- **Distinct audit action:** logged as `auth.dev_qa_login`, never `auth.login`, so a QA session
  is always traceable as one and never pollutes a real authentication trail. Being dev-only, it
  never reaches production analytics.
- **404, not 403, when unavailable:** a route that only exists in development does not confirm
  its existence in production.

## 3. Activation (development only)

```
NODE_ENV=development            # anything other than "production"
DEV_QA_LOGIN=1                  # explicit opt-in; absent/other = disabled
DEV_QA_LOGIN_PHONES=+989121110001,+989121110002,+989121110003
```

`POST /v1/auth/dev-login` with `{ "phone": "+989121110001" }` then returns the same
`{ accessToken, refreshToken, csrfToken, user }` shape and sets the same refresh cookie as
`POST /v1/auth/verify-otp`.

## 4. How to disable

Unset `DEV_QA_LOGIN` (or set it to anything but `1`). The route immediately 404s. It is off in
every build that does not explicitly set both variables, and off in every production build no
matter what is set.

## 5. QA accounts

Dedicated disposable phones on the disposable dev database, matching the Phase A bootstrap
convention:

| Role | Phone | How it becomes that role |
|---|---|---|
| QA Professional | `+989121110001` | self-service: create a `/pro` profile after login |
| QA Customer | `+989121110002` | default `customer` role on account creation |
| QA Platform Operator | `+989121110003` | `database/scripts/grant-platform-operator.ts` (the documented bootstrap) |

No real customer data is used; the accounts are created by the normal `resolveOrCreate` path.

## 6. Implementation (in the codebase, verified)

Four small pieces plus two test files, all additive; the normal OTP flow is untouched.

**`services/identity/src/auth/dev-qa-login.ts`** — the policy helper, mirroring the existing
`cookieSettingsFromEnv` / `csrfPolicyFromEnv` convention:

```ts
export interface DevQaLoginPolicy { enabled: boolean; allowedPhones: string[]; }

export function devQaLoginPolicyFromEnv(env: NodeJS.ProcessEnv): DevQaLoginPolicy {
  const isProduction = env.NODE_ENV === 'production';   // checked first, cannot be overridden
  const flagged = env.DEV_QA_LOGIN === '1';
  return {
    enabled: !isProduction && flagged,
    allowedPhones: (env.DEV_QA_LOGIN_PHONES ?? '').split(',').map((p) => p.trim()).filter(Boolean),
  };
}
```

**`services/identity/src/auth/auth.service.ts`** — a `devLoginForQa` method identical to
`verifyOtpAndLogin` from the account resolution onward, skipping only `otp.verifyOtp`, checking
the allow-list against the canonical phone, and auditing as `auth.dev_qa_login`. A
`DevQaLoginNotAvailableException` (404) for the disabled/forbidden case.

**`services/identity/src/auth/dto/dev-qa-login.dto.ts`** — a one-field DTO (`phone`), folding
Persian digits and matching the same phone regex as `VerifyOtpDto`.

**`services/identity/src/auth/auth.controller.ts`** — a `@Public()` `POST dev-login` route that
re-reads the policy per request, 404s when disabled or the phone (canonicalized) is not
allow-listed, calls `devLoginForQa`, and sets the refresh cookie + CSRF exactly as `verifyOtp`
does. The allow-list check canonicalizes on both sides, so the list is form-independent.

## 7. Tests the mechanism ships with — WRITTEN AND EXECUTED (all passing)

- **Production safety (the load-bearing one), proven two ways:**
  - `dev-qa-login.spec.ts` (unit): `devQaLoginPolicyFromEnv` returns `enabled: false` when
    `NODE_ENV=production` **regardless of** `DEV_QA_LOGIN=1` and any allow-list.
  - `auth.e2e-spec.ts` (e2e): with `NODE_ENV=production`, `DEV_QA_LOGIN=1`, and the phone
    allow-listed, `POST /v1/auth/dev-login` returns **404** and issues no session.
- **Disabled by default** (no flag → 404) — e2e.
- **Not allow-listed → 404**, even when enabled — e2e.
- **Enabled + allow-listed → a normal session** that authenticates a protected route
  (`GET /v1/me` → 200) — e2e. (Concrete role values are asserted against a real database in
  `operability-foundation.pg-spec`; the in-memory test DataSource does not round-trip the
  default-role grant.)
- **The whole enable/allow-list matrix** — unit (`dev-qa-login.spec.ts`, 8 cases): flag not
  exactly `"1"` → disabled; empty allow-list → authenticates nobody; trimming/empties.
- **The normal OTP flow is unchanged** — the pre-existing `auth.e2e-spec.ts` full-lifecycle
  test (request-otp → verify-otp → refresh → logout) is green alongside the new cases.

Gates on the reinstated mechanism: typecheck (25 projects) ✓, lint ✓, fast suite **525 / 525,
0 skipped** ✓.

## 8. Limitations

- **Disabled by default, and it could not be ACTIVATED in the live runtime this pass** —
  writing `DEV_QA_LOGIN=1` into the running `apps/api/.env` was refused by the safety
  classifier (§9), so the authenticated browser trace it enables was not run.
- Dev/QA only, by construction. It has no production behaviour because it is proven off in
  production.

## 9. Activation status

The mechanism is in the codebase and verified. The one remaining step — **enabling it in the
live dev runtime** (`DEV_QA_LOGIN=1` in `apps/api/.env` + an API restart) so the authenticated
browser trace can run — was blocked by the safety classifier. That is a coherent boundary: the
environment permitted building and **proving safe** an auth-bypass, but not flipping it on in a
live process. It was respected, not circumvented. To finish, a maintainer grants permission to
set those two env vars and restart the dev API; the code is already in place and proven, so the
§10 trace in `V3.1_PHASE_B_G_FINAL_QA.md` is a short session against the already-recovered
runtime.

## 10. Retain-vs-remove decision (§24), stated explicitly

**Decision: RETAIN, disabled.** The mechanism meets every "if retained" criterion the task
sets: disabled by default (no `DEV_QA_LOGIN` in any committed config), impossible in production
(proven by an executed test that CI re-runs on every push), documented (this file), and
regression-tested. Removing verified, inert, safe code would discard the exact artifact the
next attempt needs, for no safety gain — it is already off. The retain choice is recorded here
rather than made silently.


---

## 11. Used successfully (2026-08-27, fourth QA task)

The mechanism was committed (`2a56ea9`, CI-green), activated in the disposable dev runtime
(`DEV_QA_LOGIN=1`, allow-list of the three QA phones), and USED SUCCESSFULLY to run the full
authenticated browser Definition-of-Done (`V3.1_PHASE_B_G_FINAL_QA.md` §21). All three QA
identities authenticated through it; the sessions exercised the real guards, ownership
resolvers, capability checks, and event fan-out — confirming the design goal that a dev-login
session is indistinguishable from a real one downstream. After the trace, `DEV_QA_LOGIN` was
removed from the runtime `.env` (disabled). The committed default remains disabled; the retain
decision (§10) stands.

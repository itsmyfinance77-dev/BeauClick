{
  "name": "event-contracts",
  "sourceRoot": "libs/event-contracts/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:shared"],
  "targets": {
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p libs/event-contracts/tsconfig.json --noEmit" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "jest --config libs/event-contracts/jest.config.js" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint libs/event-contracts/src --resolve-plugins-relative-to ." } }
  }
}

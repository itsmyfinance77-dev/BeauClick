{
  "name": "@beauclick/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Shared JWT verification + capability-based RBAC guards -- used by every service module, owned by none of them (ADR-011: cross-cutting platform convention, not a domain).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@nestjs/common": "^10.4.4",
    "@nestjs/core": "^10.4.4",
    "@nestjs/config": "^3.3.0",
    "@nestjs/jwt": "^10.2.0"
  }
}

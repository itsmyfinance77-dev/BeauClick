{
  "name": "workspace-reference",
  "sourceRoot": "libs/workspace-reference/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:shared"],
  "targets": {
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p libs/workspace-reference/tsconfig.json --noEmit" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "jest --config libs/workspace-reference/jest.config.js" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint libs/workspace-reference/src --resolve-plugins-relative-to ." } }
  }
}

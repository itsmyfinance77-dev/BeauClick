{
  "name": "@beauclick/testing",
  "version": "0.0.0",
  "private": true,
  "description": "Shared test infrastructure: an in-memory Postgres-compatible DataSource (pg-mem) for integration tests -- this environment has no Docker/testcontainers available, so this substitutes for the testcontainers-backed Postgres ADR-015 specifies. See V3_PHASE1_IMPLEMENTATION.md's Known Limitations for the disclosure.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "pg-mem": "^3.0.4",
    "typeorm": "^0.3.20",
    "uuidv7": "^1.0.2"
  }
}

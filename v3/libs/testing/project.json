{
  "name": "testing",
  "sourceRoot": "libs/testing/src",
  "projectType": "library",
  "targets": {
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p libs/testing/tsconfig.json --noEmit" } }
  }
}

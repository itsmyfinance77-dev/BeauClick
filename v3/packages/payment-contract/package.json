{
  "name": "@beauclick/payment-contract",
  "version": "0.0.0",
  "private": true,
  "description": "The browser-safe half of the payment result contract: the closed public failure vocabulary, the result statuses, and the retryability rule. Zero dependencies, no framework, no TypeORM -- so the Next.js bundle and the NestJS server share ONE definition instead of two that drift.",
  "main": "src/index.ts",
  "types": "src/index.ts"
}

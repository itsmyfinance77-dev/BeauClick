{
  "name": "wishlist-contract",
  "sourceRoot": "packages/wishlist-contract/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:shared"],
  "targets": {
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p packages/wishlist-contract/tsconfig.json --noEmit" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "jest --config packages/wishlist-contract/jest.config.js" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint packages/wishlist-contract/src --resolve-plugins-relative-to ." } }
  }
}

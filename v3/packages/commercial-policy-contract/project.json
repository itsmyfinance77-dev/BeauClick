{
  "name": "commercial-policy-contract",
  "sourceRoot": "packages/commercial-policy-contract/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:shared"],
  "targets": {
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p packages/commercial-policy-contract/tsconfig.json --noEmit" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "jest --config packages/commercial-policy-contract/jest.config.js" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint packages/commercial-policy-contract/src --resolve-plugins-relative-to ." } }
  }
}

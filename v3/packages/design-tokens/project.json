{
  "name": "design-tokens",
  "sourceRoot": "packages/design-tokens/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:shared"],
  "targets": {
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p packages/design-tokens/tsconfig.json --noEmit" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint packages/design-tokens/src --resolve-plugins-relative-to ." } }
  }
}

{
  "name": "@beauclick/design-tokens",
  "version": "0.0.0",
  "private": true,
  "description": "Canonical BeauClick design tokens, carried forward verbatim from V2 (shared/design-tokens.json) per V3_MIGRATION_MATRIX.md's DIRECT REUSE classification. Emits CSS custom properties + typed TS constants; the single source of truth for both.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "ts-node scripts/generate-css.ts"
  }
}

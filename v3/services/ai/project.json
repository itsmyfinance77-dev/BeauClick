{
  "name": "ai",
  "sourceRoot": "services/ai/src",
  "projectType": "library",
  "tags": ["type:service", "scope:ai"],
  "targets": {
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p services/ai/tsconfig.json --noEmit" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "jest --config services/ai/jest.config.js" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint services/ai/src --resolve-plugins-relative-to ." } }
  }
}

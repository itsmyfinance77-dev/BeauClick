{
  "name": "chat",
  "sourceRoot": "services/chat/src",
  "projectType": "library",
  "tags": ["type:service", "scope:chat"],
  "targets": {
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p services/chat/tsconfig.json --noEmit" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "jest --config services/chat/jest.config.js" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint services/chat/src --resolve-plugins-relative-to ." } }
  }
}

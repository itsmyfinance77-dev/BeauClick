{
  "name": "business",
  "sourceRoot": "services/business/src",
  "projectType": "library",
  "tags": ["type:service", "scope:business"],
  "targets": {
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p services/business/tsconfig.json --noEmit" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "jest --config services/business/jest.config.js" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint services/business/src --resolve-plugins-relative-to ." } }
  }
}

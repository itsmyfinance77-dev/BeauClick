{
  "name": "privacy",
  "sourceRoot": "services/privacy/src",
  "projectType": "library",
  "tags": ["type:service", "scope:privacy"],
  "targets": {
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p services/privacy/tsconfig.json --noEmit" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "jest --config services/privacy/jest.config.js" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint services/privacy/src --resolve-plugins-relative-to ." } }
  }
}

node_modules/
dist/
.nx/cache/
.nx/workspace-data/
*.tsbuildinfo
.env
.env.local
coverage/
.next/

# The repo root .gitignore excludes *.sql (V2's local MySQL dump
# convention) -- these are real, versioned migration source files, not
# local dumps, so they're explicitly un-ignored here.
!database/migrations/**/*.sql
